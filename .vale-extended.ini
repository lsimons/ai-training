# Advisory-only superset of .vale.ini for `mise run prose-extended`. Same
# styles, vocabulary and package; adds the rules too noisy for every run.
# Keep the shared settings identical to .vale.ini.
StylesPath = .vale/styles
MinAlertLevel = warning
Vocab = ai-training

# Same pinned release as .vale.ini; `mise run prose-sync` fetches it.
Packages = https://github.com/errata-ai/write-good/releases/download/v0.4.1/write-good.zip, \
  https://github.com/vale-cli/proselint/releases/download/v0.3.4/proselint.zip, \
  https://github.com/vale-cli/Readability/releases/download/v0.1.1/Readability.zip, \
  https://github.com/vale-cli/Google/releases/download/v0.7.1/Google.zip, \
  https://github.com/tbhb/vale-ai-tells/releases/download/v1.37.0/ai-tells.zip

[*.{md,mdx}]
BasedOnStyles = Vale, House
# Spelling belongs to cspell (`mise run spell`, American English), so Vale's
# built-in check is off. Vale.Terms stays: it enforces the casing of the
# plain-word names in the accept list.
Vale.Spelling = NO
# A citation `(@AEC-02)` (spec S03) is syntax the remark plugin replaces
# with a number, so its key is never prose and never an acronym to spell out.
TokenIgnores = (\(@[^)]+\))


write-good.Illusions = error
write-good.Cliches = YES
write-good.Weasel = YES
write-good.ThereIs = warning
write-good.TooWordy = YES

# Sentence-initial "So". Ships as `error`. The lessons use it on purpose
# to deliver a consequence after a setup; this catches a run of them.
write-good.So = warning

proselint.Annotations = error
proselint.Cursing = error
proselint.LGBTOffensive = error
proselint.Hyperbole = error
proselint.RASSyndrome = error
proselint.Oxymorons = error
proselint.Malapropisms = error
proselint.Nonwords = error
proselint.DateCase = error
proselint.DateMidnight = error
proselint.DateRedundancy = error
proselint.DateSpacing = error
proselint.GenderBias = warning
proselint.LGBTTerms = warning
proselint.DenizenLabels = warning
proselint.GroupTerms = warning
proselint.CorporateSpeak = warning
proselint.Airlinese = warning
proselint.Jargon = warning
proselint.Archaisms = warning
proselint.Skunked = warning
proselint.Hedging = warning
proselint.Apologizing = warning
proselint.Currency = warning
# Untested here (352 swap pairs, zero hits); let it prove itself first.
proselint.Needless = warning
# Paragraph-initial "But": a device the lessons use, like "So".
proselint.But = warning

# Passive voice. A regex over "is/are/be + participle", so most hits are
# idiom ("is done", "is supposed to") or deliberate passive where the actor
# is unknown or unimportant. Worth fixing only where it hides who does what.
write-good.Passive = YES

# Google, decided rule by rule in docs/prose/README.md. Most of the package
# is house style for reference documentation; what runs here is the part the
# project adopted (present tense, contractions, the Oxford comma, sentence
# case headings) plus the tripwires. The project's own House style holds the
# one rule Google has backwards for this repository: punctuation goes
# outside quotation marks, so a quoted prompt never seems to end in a comma.
#
# A hit is a defect every time: "3pm", slash dates, a spaced dash, "he/she",
# "A.B.C.", a period ending a heading, "tl;dr".
Google.AMPM = error
Google.DateFormat = error
Google.EmDash = error
Google.Gender = error
Google.Periods = error
Google.HeadingPunctuation = error
Google.Slang = error
# Adopted house style, advisory. Present tense over "will"; contractions;
# the Oxford comma; sentence-case headings (proper nouns and codes that may
# stay capitalized go in accept.txt, which this rule honors); "for example"
# over "e.g."; no "currently" or "latest" (they rot); no "!" in teaching
# text; no "(s)".
Google.Will = warning
Google.Contractions = warning
Google.OxfordComma = warning
Google.Headings = warning
Google.Latin = warning
Google.Timeless = warning
Google.Exclamation = warning
Google.OptionalPlurals = warning
House.Quotes = warning
# Off: Passive (identical to write-good.Passive hit for hit), Quotes (the
# American convention; House.Quotes is the project's), Parens, Colons,
# WordList, WordListCase, Ellipses, Anthropomorphism (labels, product
# vocabulary and quoted templates), Spacing and LyHyphens (every hit a false
# positive: "DeepLearning.AI", "supply-chain"), GenderBias and Jargon
# (proselint's run already), Spelling (cspell's job). We, FirstPerson,
# Semicolons, ExcessiveClaims, Units, Ordinal and Ranges live in
# .vale-extended.ini.
# Second person for guides and tutorials is the aim; "we" and "I" are a
# now-and-then sweep because the lesson opener and the tutor commands use
# them on purpose. The rest measure density (semicolons per lesson) or
# fire on a word that is usually fine ("best", "2h", "2nd", "from 1-5").
Google.We = warning
Google.FirstPerson = warning
Google.ExcessiveClaims = warning
Google.Units = warning
Google.Ordinal = warning
Google.Ranges = warning

# ai-tells (tbhb/vale-ai-tells), decided rule by rule in docs/prose/README.md.
# The package ships all 137 rules at `error`; here each is named with its
# own level. The content of this repository is written by agents, and the
# maintainer wants less of the register these rules catch, so the rules
# that fired are warnings to be fixed, and a rule reads zero in the
# lessons it ratchets to `error` there (the scoped block below).
#
# Zero hits and nobody types the phrase by accident: an unspaced dash, "--"
# in prose, "I hope this helps", "In today's fast-paced", "In conclusion",
# "Let's unpack", "despite these challenges", "The result? ...", "rich
# tapestry", "real consequences", "nestled in", "cannot be overstated", and
# the listicle headings.
ai-tells.EmDashUsage = error
ai-tells.DoubleHyphen = error
ai-tells.ClosingPleasantries = error
ai-tells.OpeningCliches = error
ai-tells.ConclusionMarkers = error
ai-tells.UnpackExplore = error
ai-tells.DespiteChallenges = error
ai-tells.RhetoricalSelfAnswer = error
ai-tells.AICompoundPhrases = error
ai-tells.PromotionalPuffery = error
ai-tells.UrgencyInflation = error
ai-tells.WrapUpHeadings = error
ai-tells.MarketingHeadings = error
ai-tells.AnnouncementHeadings = error
ai-tells.ListIntroductions = error
# Zero hits today; a hit deserves a look but may be deliberate.
ai-tells.RestatementMarkers = warning
ai-tells.HollowAcknowledgment = warning
ai-tells.ResonateOveruse = warning
ai-tells.StrategyBuzzwords = warning
ai-tells.AIAdjectiveNounPairs = warning
ai-tells.AbsoluteAssertions = warning
ai-tells.VagueAttributions = warning
ai-tells.DefensiveHedges = warning
ai-tells.FalseBalance = warning
ai-tells.FalseExclusivity = warning
ai-tells.IncompleteComparison = warning
ai-tells.StrawmanContrast = warning
ai-tells.RedundantPrecaution = warning
ai-tells.StackedHedges = warning
ai-tells.NegatedPair = warning
ai-tells.UniversalObject = warning
ai-tells.OverusedVocabularyVerbs = warning
ai-tells.MicDropHeadings = warning
ai-tells.NarrativePivots = warning
ai-tells.SummativeAppositive = warning
ai-tells.RhetoricalDevices = warning
ai-tells.EmptyPaddingStacked = warning
ai-tells.NamedAdjective = warning
ai-tells.NominalizedScopeChange = warning
ai-tells.OrganicConsequence = warning
ai-tells.ParticipialPadding = warning
ai-tells.ScopePartition = warning
ai-tells.ServesAsDodge = warning
ai-tells.ShellNounCopula = warning
ai-tells.UnderRegime = warning
# The register: figurative verbs on inanimate subjects ("lives in", "shape",
# "carries", "worth"), a comma-free clause after a semicolon, a count that
# announces a list, "no X, no Y", "a Y, not a Z", the clipped motto, the
# consequence tail. These fired, the text was swept, and what remains
# either quotes source material or is a false match noted in the summary.
ai-tells.SemicolonUsage = warning
ai-tells.CataphoricForecasting = warning
ai-tells.FigurativeShape = warning
ai-tells.FigurativeWorth = warning
ai-tells.FigurativeCarries = warning
ai-tells.FigurativeLives = warning
ai-tells.FigurativeStays = warning
ai-tells.FigurativeWins = warning
ai-tells.FigurativeOwns = warning
ai-tells.FigurativeSits = warning
ai-tells.FigurativeSurfaces = warning
ai-tells.FigurativeDisguises = warning
ai-tells.FigurativeLands = warning
ai-tells.FigurativeNouns = warning
ai-tells.FigurativeQuantities = warning
ai-tells.FigurativeBreeds = warning
ai-tells.FigurativeCasts = warning
ai-tells.FigurativeClears = warning
ai-tells.FigurativeDemands = warning
ai-tells.FigurativeEarns = warning
ai-tells.FigurativeFalls = warning
ai-tells.FigurativeFires = warning
ai-tells.FigurativeHolds = warning
ai-tells.FigurativeLends = warning
ai-tells.FigurativeLoud = warning
ai-tells.FigurativeMints = warning
ai-tells.FigurativePays = warning
ai-tells.FigurativeQuiet = warning
ai-tells.FigurativeReaches = warning
ai-tells.FigurativeRides = warning
ai-tells.FigurativeRuns = warning
ai-tells.FigurativeSees = warning
ai-tells.FigurativeSettles = warning
ai-tells.FigurativeStrikes = warning
ai-tells.FigurativeSweeps = warning
ai-tells.FigurativeTravels = warning
ai-tells.FigurativeTrips = warning
ai-tells.MotionMetaphors = warning
ai-tells.GrowthMetaphors = warning
ai-tells.JourneyMetaphors = warning
ai-tells.FusionMetaphors = warning
ai-tells.DepletionMetaphors = warning
ai-tells.EvasionMetaphors = warning
ai-tells.MortalityMetaphors = warning
ai-tells.AnthropomorphicJustification = warning
ai-tells.AnthropomorphicAdjectives = warning
ai-tells.ContrastiveNegation = warning
ai-tells.ContrastiveFormulas = warning
ai-tells.NegatedSubject = warning
ai-tells.NegatedObject = warning
ai-tells.UniversalSubject = warning
ai-tells.MicDrop = warning
ai-tells.StackedAnaphora = warning
ai-tells.ParallelStaccato = warning
ai-tells.CoordinatedReveal = warning
ai-tells.PseudoCleft = warning
ai-tells.EmphaticCopula = warning
ai-tells.PointVerdict = warning
ai-tells.AbstractionSubject = warning
ai-tells.ConsequenceClause = warning
ai-tells.ConsequenceParticiple = warning
ai-tells.ExplainerLeads = warning
ai-tells.FillerIntensifier = warning
ai-tells.FillerPhrases = warning
ai-tells.EmptyPadding = warning
ai-tells.NounString = warning
ai-tells.HedgingPhrases = warning
ai-tells.SycophancyMarkers = warning
ai-tells.ColloquialAssessments = warning
ai-tells.Metacommentary = warning
ai-tells.OverusedVocabulary = warning
ai-tells.SelfReference = warning
ai-tells.StructureAnnouncements = warning
# Three rules run as House copies instead (.vale/styles/House/, warning by
# default): Transitions is FormalTransitions without "for example" and
# case-sensitive, so only the sentence-initial "Hence" or "Notably" fires;
# VerbTricolon is the package rule without the after-colon and -s tokens,
# which matched noun lists; Idioms is FigurativeIdioms without "blast
# radius", the safety lesson's defined term, which the package rule can't
# exempt because it has no `exceptions` key.
#
# Off: the rules that flag this project's own vocabulary or conventions.
# AnthropomorphicCognition (34 of 42 hits are "teaches"; a lesson teaches),
# ShipOveruse (Ship is a development phase here), BareNames and BareReaches
# ("names what an action can reach" is a learning objective), LabelAndExplain
# ("The rule: ..." closes every Pitfall, per spec S03), FormalRegister (10 of
# 12 hits are "implement", software vocabulary), HouseStyle (the project's
# own style is named House), EnforcementMetaphors ("gate" is the CI
# vocabulary), FigurativeDraws ("Draws on topics" is a spec column),
# FigurativeKeeps ("keep it short"), BareHolds (the YAML calls it the loudest
# rule and says to disable it where the container sense is kept),
# ExplainerHeadings ("Where a rule can go" is the right heading),
# AffirmativeFormulas and SequencingMarkers (both fire on "The first lesson",
# the project's noun), VerbTricolonDensity (its parent is a House copy),
# ColonUsage (the specs' "**Purpose:**" label, product names; Google.Colons
# is off for the same reason).

# Readability: seven whole-file scores that agree with each other at 0.89 or
# better, so one is enough. Flesch-Kincaid grade, threshold 8 (set inside the
# package), lessons only: that is the material with a reader whose time the
# score measures. On the plan notes it fires on every file forever. One
# alert per file at line 1; find the long sentences yourself, or run
# `mise run prose-metrics -- Readability` for every file's score.
[site/src/content/docs/**/*.{md,mdx}]
# The citation pattern again (a scoped section replaces the parent's
# TokenIgnores), plus the one quoted statutory term a lesson must use as
# written: Article 5(1) of the EU AI Act bans practices that cause
# "significant harm", and the lesson quotes it. Only the exact quoted
# phrase is ignored, so a bare "significant" still fails.
TokenIgnores = (\(@[^)]+\)), ("significant harm")
Readability.FleschKincaid = warning
# The ratchet. Every ai-tells register rule and House copy that read zero
# in the lessons after the sweep is an error here, so new lesson text
# can't bring the pattern back. The three left out (SemicolonUsage,
# CataphoricForecasting, NegatedObject) still match inside illustrative
# <Prompt> and <Response> transcripts and stay warnings. When one of
# those reaches zero, add it.
ai-tells.FigurativeShape = error
ai-tells.FigurativeWorth = error
ai-tells.FigurativeCarries = error
ai-tells.FigurativeLives = error
ai-tells.FigurativeStays = error
ai-tells.FigurativeWins = error
ai-tells.FigurativeOwns = error
ai-tells.FigurativeSits = error
ai-tells.FigurativeSurfaces = error
ai-tells.FigurativeDisguises = error
ai-tells.FigurativeLands = error
ai-tells.FigurativeNouns = error
ai-tells.FigurativeQuantities = error
ai-tells.FigurativeBreeds = error
ai-tells.FigurativeCasts = error
ai-tells.FigurativeClears = error
ai-tells.FigurativeDemands = error
ai-tells.FigurativeEarns = error
ai-tells.FigurativeFalls = error
ai-tells.FigurativeFires = error
ai-tells.FigurativeHolds = error
ai-tells.FigurativeLends = error
ai-tells.FigurativeLoud = error
ai-tells.FigurativeMints = error
ai-tells.FigurativePays = error
ai-tells.FigurativeQuiet = error
ai-tells.FigurativeReaches = error
ai-tells.FigurativeRides = error
ai-tells.FigurativeRuns = error
ai-tells.FigurativeSees = error
ai-tells.FigurativeSettles = error
ai-tells.FigurativeStrikes = error
ai-tells.FigurativeSweeps = error
ai-tells.FigurativeTravels = error
ai-tells.FigurativeTrips = error
ai-tells.MotionMetaphors = error
ai-tells.GrowthMetaphors = error
ai-tells.JourneyMetaphors = error
ai-tells.FusionMetaphors = error
ai-tells.DepletionMetaphors = error
ai-tells.EvasionMetaphors = error
ai-tells.MortalityMetaphors = error
ai-tells.AnthropomorphicJustification = error
ai-tells.AnthropomorphicAdjectives = error
ai-tells.ContrastiveNegation = error
ai-tells.ContrastiveFormulas = error
ai-tells.NegatedSubject = error
ai-tells.UniversalSubject = error
ai-tells.MicDrop = error
ai-tells.StackedAnaphora = error
ai-tells.ParallelStaccato = error
ai-tells.CoordinatedReveal = error
ai-tells.PseudoCleft = error
ai-tells.EmphaticCopula = error
ai-tells.PointVerdict = error
ai-tells.AbstractionSubject = error
ai-tells.ConsequenceClause = error
ai-tells.ConsequenceParticiple = error
ai-tells.ExplainerLeads = error
ai-tells.FillerIntensifier = error
ai-tells.FillerPhrases = error
ai-tells.EmptyPadding = error
ai-tells.NounString = error
ai-tells.HedgingPhrases = error
ai-tells.SycophancyMarkers = error
ai-tells.ColloquialAssessments = error
ai-tells.Metacommentary = error
ai-tells.OverusedVocabulary = error
ai-tells.SelfReference = error
ai-tells.StructureAnnouncements = error
House.Transitions = error
House.VerbTricolon = error
House.Idioms = error
# Acronyms asks each file to spell out an acronym once, "Term (ACR)".
# Lessons only: the plan notes and specs are full of codes that are not
# acronyms. The ones this audience reads without help are exempted in
# accept.txt; MCP, RAG and LLM are deliberately not, so the lesson that
# introduces them defines them.
Google.Acronyms = warning
# Every semicolon in a lesson, for the sweep that splits long sentences.
Google.Semicolons = warning

[*.{yaml,yml}]
BasedOnStyles = Vale
Vale.Spelling = NO
# The bibliography's `url` values are URLs, and a URL path is lowercase by
# convention (`.../with-anthropic/`, `cs50.harvard.edu`), so Vale.Terms
# must not read them as miscased names. `TokenIgnores` only applies to
# markup formats, and Vale reads YAML as plain text (`[formats]` cannot
# remap a known extension), so `mise run prose-extended` passes the YAML
# files with `--ext=.md`. Only Vale.Terms runs on them, so the Markdown
# parsing changes nothing else. The second pattern is the `Academy <slug>`
# citation key (spec S02): the slug is a lowercase URL path segment
# (`introduction-to-claude-cowork`), so it is not a miscased name either.
TokenIgnores = (https?://\S+), (Academy [a-z0-9-]+)
