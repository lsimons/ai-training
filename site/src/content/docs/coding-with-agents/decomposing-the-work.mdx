import { Choice, MultiChoice, Order, Predict, Sort, Pitfall, Exercise, Recap } from '@components/lesson';

The to-do program gets a feature: "Let me import my old list. I have a
text file with one task per line, and I don't want to type forty `add`
commands." The last lesson decided to build it and wrote
the success criteria. In this lesson we take the feature apart before any
agent sees it. We split it into components with one responsibility each,
we draw the dependencies between them and put them in order, and we write
the check for the first component before it exists. Then we run that
check and watch it fail, which is the state the agent starts from.

An agent given a whole feature makes the split itself, and it makes a
different one every session. It also builds the parts together and
tests them at the end, so a mistake in the first part is hidden by the
second and found by the third. Brilliant's skills map for coding with AI
lists decomposition, dependency analysis, and designing the verification as
skills of the engineer (@Brilliant SPC), and Claude Academy's playbook for
an AI-native development process writes the intent and the check down
before the code (@Academy ai-native-sdlc-playbook). Both put the split in
your hands. The agent gets one component at a time, with its check.

## The fixture

The fixture is the to-do program from *Your first session with a coding
agent*, some weeks later. It has grown a `clear` command and been split
into three modules: `todo.py` holds the commands, `store.py` reads and
writes the list, and `render.py` formats it. It is in the course repository
under `site/examples/coding-with-agents/decomposing-the-work/fixture-repo/`.
Copy that directory to a place of its own and change into it. A copy has
no git history, so the reset is to delete the copy and copy again. Working
inside a clone of the course repository also works, and there
`git checkout -- .` is the reset, but the agent then reads the course's own
instruction files from the directories above the fixture. The copy is the
better default.

Run the program before you plan anything, so you know what the list looks
like now.

<Predict id="run-list" title="Show the list"
  answer={`1. [ ] Buy milk
  2. [x] Call the plumber
  3. [x] Renew the passport
  4. [ ] Water the plants
  2 open, 2 done`} run="coding-with-agents/decomposing-the-work/list.py">
Run this, and compare what you see with the output below.

```sh
python3 todo.py list
```
</Predict>

The suite passes, and nothing in it changes until you add the first test of
this lesson.

<Predict id="run-tests" title="Run the tests"
  answer="OK" run="coding-with-agents/decomposing-the-work/tests.py">
Run the tests and keep only the last line of the output.

```sh
python3 -m unittest -q 2>&1 | tail -1
```
</Predict>

The examples in this lesson aren't graded. Each one is a command you run
in the fixture, with the output the course itself checks on every build.
The checkpoints ask about the split and the check, because making those is
what this lesson teaches.

## One responsibility per component

The success criteria from the last lesson, shortened: `todo import tasks.txt`
reads a text file with one task per line, a line that starts with `x ` is
a done item, blank lines are skipped, a task already in the list isn't
added twice, and the command prints how many items it imported and how
many it skipped. `add`, `list`, `done` and `clear` keep working.

The feature reads as one thing. It is three. A component has one
responsibility, an interface you can state in a sentence, and a size that
fits one agent session and one change you can review in a few minutes. Cut
the feature where the data changes form: text lines become items, items
are merged into the list, and the list is shown to the user.

| Component | Responsibility | Interface |
| --------- | -------------- | --------- |
| Parser | Turns text lines into items | `importer.parse_lines(lines)` returns a list of items, each `{"text": ..., "done": ...}` |
| Merge | Adds new items to the list and leaves duplicates out | `importer.merge(items, new_items)` appends the items whose text isn't in `items` and returns the count added |
| Command | Wires `todo import <file>` to the two above and reports | `todo.py` reads the file, calls the parser and the merge, prints `imported N items, skipped M` |

Each row answers three questions with one sentence each. If a row needs
two sentences for its responsibility, it is two components. If the interface
sentence has to describe the code inside, the boundary is in the wrong
place. Trailing spaces and the `x ` mark are the parser's
problem and nobody else's, and "already in the list" is the merge's
problem, so the command has nothing to decide.

Write the list before the brief, and put it in the brief. The agent builds
the parser to the interface you named, and the merge you brief next week
builds against the same interface, whether the same agent writes it or
not.

<MultiChoice id="one-responsibility" objective="coding-with-agents/specifies-work/decomposes-into-components"
  concepts={['decomposition-into-components']}
  context="A coding agent is going to add an import command to a small command-line to-do program. The feature reads a text file with one task per line, skips blank lines, treats a leading x as a done mark, leaves out tasks already in the list, and prints how many it imported and skipped. The learner is splitting it into components."
  title="Which of these are components with one responsibility?" hint="Can you state what the component does, and what it takes and returns, in one sentence each?"
  options={[
    { text: 'A function that turns a list of text lines into a list of items, handling the done mark and the blank lines.', correct: true },
    { text: 'A function that reads the file, parses the lines, skips duplicates and prints the report.', why: 'That is the whole feature with a function name on it. Its interface needs a paragraph, and a mistake in the parsing is hidden by the reporting.' },
    { text: 'A function that appends the items not yet in the list and returns how many it added.', correct: true },
    { text: 'A function that parses the lines and also prints how many were blank.', why: 'Two responsibilities: turning lines into items, and reporting to the user. The report belongs to the command, and the parser then has a reason to change when the output format does.' },
  ]}>
Which of these are components with one responsibility each?
</MultiChoice>

<Sort id="whose-problem" phase="review" objective="coding-with-agents/specifies-work/decomposes-into-components"
  concepts={['decomposition-into-components']}
  context="A coding agent is going to add an import command to a small command-line to-do program in three components: a parser that turns text lines into items, a merge that adds new items to the list and leaves duplicates out, and the command that reads the file, calls both and prints a report."
  title="Whose problem is it?" hint="Which component sees this detail first, and which one never needs to know it?"
  buckets={['Parser', 'Merge', 'Command']}
  items={[
    { text: 'A line that ends in three spaces', bucket: 0 },
    { text: 'A task text that is already in the list', bucket: 1 },
    { text: 'The file named on the command line does not exist', bucket: 2 },
    { text: 'The wording of the imported-and-skipped report', bucket: 2 },
    { text: 'A line that starts with x and a space', bucket: 0 },
  ]} />

## Order the work by dependency

Three components, and they can't be built in any order. The merge takes
items, and the item format is the parser's output. The command calls both.
Draw the arrows. The parser has no dependency. The merge depends on the
parser's format, and the command depends on both. That order is the plan,
and each step is checked before the next one starts.

The order also names the risky assumption. Everything rests on the item
format the parser produces. If the parser is wrong about what an item is,
the merge inherits the mistake and the command shows it. So the parser
goes first. It is checked alone with its own test, and only then is the
merge briefed. A component that passes its own check becomes solid ground for
the next one.

The dependency drawing also shows which components have no arrow between them. If the
feature had a fourth component, a `render` change that shows how many
items came from an import, it would depend on the command and not on the
parser, and a second agent session could build it in parallel once the
command's interface was fixed. Naming the dependencies is what tells you
what can run side by side and what has to wait.

<Pitfall title="The merge inherited the parser's mistake">
An engineer briefed the parser and the merge together. The agent built
both, tested the merge by feeding it items it had typed by hand, and
reported green. The parser kept the trailing spaces on each line, so an
imported `Buy milk ` and the existing `Buy milk` were two different texts,
and the merge added the duplicate. The merge's tests never saw a parsed
item, so they passed, and the bug showed up as a doubled list in the
command a week later, where nobody was looking at the parser. The rule:
check each component alone before the next one is built on it, and
feed the second component's test what the first one produces.
</Pitfall>

<Order id="dependency-order" objective="coding-with-agents/specifies-work/decomposes-into-components"
  concepts={['dependencies']}
  context="A coding agent is adding an import command to a small command-line to-do program in three components: a parser that turns text lines into items, a merge that adds new items to the list and leaves duplicates out, and the command that reads the file, calls both and prints a report. Each component is checked before the next is briefed."
  title="Put the steps in dependency order" hint="Which component's output does each of the others take as input?"
  steps={[
    'Brief the parser and check that its test passes',
    'Brief the merge against the parser\'s item format and check its test',
    'Brief the command that reads the file and calls both',
    'Run the whole import against a copy of the list',
  ]} />

<Choice id="what-rests-on-what" phase="review" objective="coding-with-agents/specifies-work/decomposes-into-components"
  concepts={['dependencies']}
  context="A coding agent is adding an import command to a small command-line to-do program in three components: a parser that turns text lines into items, a merge that adds new items to the list and leaves duplicates out, and the command that reads the file, calls both and prints a report."
  title="Which component is built first?" hint="Which component's output is another component's input, and which one has no input from the others?"
  options={[
    { text: 'The command, because it is what the user sees, and the parser and merge can be filled in behind it.', why: 'The command calls both of the others. Built first, it has nothing to call, and its check can only pass against stubs.' },
    { text: 'The merge, because duplicates are the part most likely to go wrong.', why: 'The merge takes the parser\'s items as input. Built first, it is tested on items typed by hand, and a parser mistake reaches it later unseen.' },
    { text: 'The parser, because the other two take its output as input.', correct: true },
    { text: 'Any of them, because the agent runs the whole suite at the end anyway.', why: 'A suite run at the end tells you something failed, and three components built together give it three places to look. The order is what makes each failure point at one component.' },
  ]}>
Which component do you brief first, and why?
</Choice>

## Design the check before the code

The parser doesn't exist yet, and this is the moment to decide how you
know it is right. Write the acceptance test now, put it in the suite,
and name it in the brief as the command that means done. The agent then
has a target it can run itself, and "done" means the line reads `OK`
instead of the agent saying so.

The test has to do one more thing: it has to fail on a stub. A parser that
returns an empty list, or that returns every line as an open item, is the
kind of first draft an agent writes when the target is vague. One input
with a blank line, a done mark and a line with spaces around it is enough
to reject each of those. Create `test_importer.py` with this content.

```python
import unittest

import importer


class ImporterTests(unittest.TestCase):
    def test_parse_lines_keeps_text_and_done_mark_and_skips_blank_lines(self):
        lines = ["Buy milk", "", "x Call the plumber", "  Water the plants  "]
        self.assertEqual(
            importer.parse_lines(lines),
            [
                {"text": "Buy milk", "done": False},
                {"text": "Call the plumber", "done": True},
                {"text": "Water the plants", "done": False},
            ],
        )
```

Run the suite. `importer.py` doesn't exist, so the test can't even import
it, and the suite reports an error.

<Predict id="run-failing-test" title="Run the suite before the parser exists"
  answer="FAILED (errors=1)" run="coding-with-agents/decomposing-the-work/failing_test.py">
With `test_importer.py` in place, run the tests and keep the last line. The
course checks this against its own copy of the test, which is the one
shown above.

```sh
python3 -m unittest -q 2>&1 | tail -1
```
</Predict>

Now prove the test does its job. Create `importer.py` with a stub that
has the right name and does nothing, and run the suite again.

```python
def parse_lines(lines):
    return []
```

<Predict id="run-stub-test" title="Run the suite against a stub"
  answer="FAILED (failures=1)" run="coding-with-agents/decomposing-the-work/stub_test.py">
With the stub in place, run the tests and keep the last line. The course
checks this against its own copy of the stub.

```sh
python3 -m unittest -q 2>&1 | tail -1
```
</Predict>

An error became a failure: the module imports, and the assertion rejects
what it returns. Delete `importer.py` again. The agent writes the real one. Your
brief for the first component now fits in a few lines: the parser's
responsibility and interface from the table, the file it may create, and
the command that means done.

```text
Add importer.py with parse_lines(lines), which turns text lines into
items as test_importer.py describes. Create only importer.py. Done when
`python3 -m unittest -q` prints OK. Do not change the test.
```

An agent that can't make the test pass may change
the test until it does, and then the check doesn't check anything. The
last line of the brief closes that door.

<Choice id="fails-on-a-stub" objective="coding-with-agents/specifies-work/designs-the-check"
  concepts={['designing-the-verification']}
  context="Before a coding agent builds a parser that turns text lines into to-do items, the learner writes an acceptance test for it. The test should reject a stub, a version of the parser that has the right function name and does no real work."
  title="Which test would fail on a stub?" hint="Imagine a parser that returns an empty list, and one that returns every line as an open item. Which test rejects both?"
  options={[
    { text: 'Assert that `parse_lines` on the same four lines returns a list and not `None`.', why: 'A stub that returns an empty list passes. The test checks the type and never the work.' },
    { text: 'Assert that `parse_lines` on the same four lines runs to the end without raising an error.', why: 'Every stub passes this. A function that does nothing raises nothing.' },
    { text: 'Assert that `parse_lines` on four lines, one blank and one marked done, returns three items with the right texts and marks.', correct: true },
    { text: 'Assert that `importer.py` exists in the directory and defines a function called `parse_lines`.', why: 'That is what a stub is: the name without the work. The check would pass on the file you just deleted.' },
  ]}>
Which of these tests would fail on a stub parser?
</Choice>

<MultiChoice id="what-the-brief-names" phase="review" objective="coding-with-agents/specifies-work/designs-the-check"
  concepts={['designing-the-verification']}
  context="A learner has split an import feature for a small to-do program into components and written an acceptance test for the first one, a parser, before it exists. They are briefing a coding agent to build that one component."
  title="What does the brief for one component name?" hint="What lets the agent check its own work without you, and what stops it from moving the target?"
  options={[
    { text: 'The command that means done, such as the test run that has to print OK.', correct: true },
    { text: 'The one file the agent is allowed to create or change for this component.', correct: true },
    { text: 'That the test file stays as it is.', correct: true },
    { text: 'The whole feature, so the agent sees the big picture while it builds the first part.', why: 'The agent then builds toward the whole and the first component grows a second responsibility. The brief for the merge comes when the parser is checked.' },
    { text: 'That the agent should write its own test when it is finished.', why: 'A test written after the code asserts whatever the code does. The check that counts was written before, and it is already in the suite.' },
  ]}>
Which of these belong in the brief for the parser?
</MultiChoice>

<Exercise stretch="Brief the agent for the parser with the brief above, in a fresh session, and read the diff: it should touch `importer.py` and nothing else.">
Reset the fixture and take the import feature apart yourself, without
looking at the table above. Write the components as a list with one
responsibility and one interface sentence each, draw the dependency
arrows between them, and put them in build order. Then write the failing
acceptance test for the first component, run
`python3 -m unittest -q`, and check that the last line says the test
failed. Add a stub with the right name and run it again, and the line
must still say it failed. Fifteen lines of notes and one test file is
enough. You do this so that the agent builds one checked component at a
time and never gets to choose the split.

A good result: each component's interface fits in a sentence, the first
one in your order is the one the others take input from, the test fails
before the component exists and fails again on the stub, and the brief
names the test run as what done means. Reflect: which of your components
would you have merged if the agent had been given the whole feature, and
what mistake could then have hidden inside it?
</Exercise>

<Recap>
1. A component has one responsibility, an interface that fits in a sentence, and a size that fits one agent session and one reviewable change. Cut where the data changes form (@Brilliant SPC).
2. Dependencies set the build order. The component whose output the others take is built and checked first, so a mistake in it never reaches the next one unseen.
3. A component checked alone is solid ground. Feed the next component's test what the first one produces, or the second inherits and hides the first one's mistakes.
4. Write the acceptance test before the component exists, put it in the suite and name the test run in the brief as what done means (@Academy ai-native-sdlc-playbook).
5. A check that passes on a stub doesn't check the work. Run it against a version with the right name and no work, and the line must still say it failed.
</Recap>
