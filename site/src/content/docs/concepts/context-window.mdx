import { Choice, Sort, Predict, Pitfall, Exercise, Recap, Prompt, Response, MultiChoice, Scenario } from '@components/lesson';

You have been working with a chat assistant on a report for two hours. In
your first message you said the report is for the finance team and must
stay under two pages. Now you ask for the closing section, and the answer
runs to four pages and addresses "the engineering team". The transcript
below is illustrative, written for this lesson rather than recorded from a
model, but the pattern is one you will meet.

<Prompt model="illustrative" recorded="illustrative">
Write the closing section now. Same audience and length as we agreed.
</Prompt>
<Response>
Here is the closing section for the engineering team. I have kept the
technical detail you asked for, so it runs to about four pages. Let me know
if you want the deployment appendix as well.
</Response>

The model did not forget in the way a person forgets. It never saw your
first message when it wrote this answer. In the previous lesson you learned
that a model reads tokens and predicts the next one from everything it has
seen so far. This lesson is about the limit on "everything it has seen so
far", the **context window**. In this lesson we measure a page of text in
tokens and work out how many pages a window holds, then watch a chat grow
past it.

## Count the tokens of a page

A context window is measured in tokens, so the first step is to get a
feel for how many tokens a page of text is. The script is
`site/examples/concepts/context-window/count_tokens.py` in the repository
of this course. Save a copy and run it with no arguments. It counts a
built-in sample of meeting notes,
about half a page. On a fresh Mac the first `python3` command may offer to
install the command-line developer tools, and that is expected.

<Predict id="count-the-sample" title="Count the sample"
  answer={`characters: 460
words: 80
tokens (estimate): 115`} run="concepts/context-window/count_tokens.py">
Run the counter on its built-in sample and compare what you see with the
output below.

```sh
python3 count_tokens.py
```

</Predict>

The script says "estimate" for a reason. It applies the common rule of
thumb for English, about four characters per token. A word of up to six
letters counts as one token and a longer word as one token per four
letters, with each punctuation mark counted as a token of its own. Real
tokenizers are more detailed than that, and every vendor has its own, so
the exact count for a model comes from that vendor. A call to the model
through the vendor's programming interface (API) returns the number of
tokens it used along with the answer, and vendors publish a token-counting
call or a web page that runs their tokenizer. For planning, the rule of
thumb is close enough: a full page of English text is a few hundred tokens.

## How a tokenizer splits text

The counter cuts "delivery" into two tokens by its four-letter rule. That
rule is a stand-in for what a real tokenizer does. A real tokenizer has a
fixed vocabulary of tens of thousands of chunks learned from training text,
and it cuts where that vocabulary happens to have a chunk. A common word
like "delivery" is in the vocabulary whole and costs one token, however
long it is. A rare word or a name is split into pieces instead, often at
places that have nothing to do with syllables or spelling. "Chidi" from the
sample is two tokens in current tokenizers, and a word in another language
may end up as one token per letter (@AEC-02). The counter cannot know which
words are common, so it applies one rule to all of them.

Asking a model how many letters "strawberry" has goes wrong so often for
the same reason. The model receives the token for "strawberry" as one
number, and the letters are not in its input, so it predicts a count from
what it has seen written about the word, the same way it predicts any other
answer. Counting characters yourself, as the script does, is the reliable
way.

## How many pages fit

Take the token count from the first step and see how many such pages fit
in windows of different sizes. The second script takes the count as its
argument.

<Predict id="pages-per-window" title="Pages per window"
  answer={`one page: 115 tokens
8,000-token window: 69 pages
32,000-token window: 278 pages
200,000-token window: 1,739 pages
1,000,000-token window: 8,695 pages`} run="concepts/context-window/pages_per_window.py">
Run the second script with the token count of the sample. What does it
print?

```sh
python3 pages_per_window.py 115
```

</Predict>

The window sizes are round numbers that stand for the sizes on the market
in 2026. The 8,000 row is the size of a few years ago, kept for the
contrast. Small models that run on a laptop have windows at the low end
today, and the large vendor models offer 200,000 tokens or more (@AEC-02).
At the top end the window holds a few thousand pages of text, which sounds
like more than you will ever need. The next section shows why it still runs
out.

A big window is not free to fill. The model processes every token in the
window for every response, which makes a fuller window cost more per answer
and take longer to answer. Nor does the model weigh every part of a full
window equally well: a fact in the middle of a long paste counts for less
in the prediction than the same fact at the top or at the bottom, as the
previous lesson noted under instruction dilution
(@Academy ai-capabilities-and-limitations).

## What the window holds

The window is not only your messages. For one response, the model sees all
of this at once, and the total must fit:

- The instructions the product or your team set up, which you may never
  see. In a chat product these are often a few pages by themselves.
- The conversation so far, with every message from you and every answer.
- Everything you pasted or attached, in full. A document you attach is
  turned into tokens like any other text.
- Results from tools the assistant used, such as a web search or a file
  that it read.
- The answer it is producing, token by token, since each new token is
  predicted from the whole window including the answer so far.

From this follows the one rule of the lesson: anything outside the window
does not exist for the model. Yesterday's chat, the file you did not paste,
and the company wiki are all invisible unless something puts them into the
window for this response (@Academy ai-capabilities-and-limitations). The
same rule explains why a model "remembers" nothing between conversations,
because the weights do not change when you type and so the window is the
only place a fact can be.

<Sort id="inside-the-window" objective="concepts/explains-models/explains-generation"
  concepts={['context-window']}
  title="What can the model see?"
  hint="For each item, ask: is it in the text being sent to the model for this one response?"
  buckets={['In the window for this response', 'Does not exist for the model']}
  items={[
    { text: 'The instructions the product set up before your first message', bucket: 0 },
    { text: 'The paragraph you pasted two messages ago in a chat that is still short', bucket: 0 },
    { text: 'The first half of the answer the model is writing right now', bucket: 0 },
    { text: 'The result of the web search the assistant just ran', bucket: 0 },
    { text: 'A chat you had with the same assistant last week, with memory features off', bucket: 1 },
    { text: 'A spreadsheet on your laptop that you mentioned by name', bucket: 1 },
    { text: 'A page of the company wiki that nobody pasted into the chat', bucket: 1 },
  ]} />

## When the early facts fall out

Your chat with the assistant grows with every turn, and so does the text
sent to the model for each response. A long chat with pasted documents can
pass a 200,000-token window in an afternoon. Products handle the overflow
in different ways. Some silently drop the oldest turns, some replace them
with a short summary, and some refuse to continue and ask you to start a
new chat. In the first two cases you get the report chat from the top of
this lesson: the answers are still fluent, but a fact from the start of the
chat has left the window.

<Pitfall title="Scolding the model for forgetting">
Two hours into the report chat, the assistant addresses the wrong team. You
reply: "I told you at the start this is for finance. Pay attention." The
next answer apologizes and then makes the same kind of mistake with the
page limit, because your correction named only the audience. The model
never saw your first message, so the scolding gave it nothing to work
with. The rule: when a long chat contradicts itself, restate the facts that
matter, in the message you are sending now. "This section is for the
finance team, two pages at most, plain language" puts the facts back in the
window, and the next answer has them.
</Pitfall>

Restating the facts is the fix in the moment. To prevent the problem, keep
the facts the model needs every time in one message you can paste again,
and start a fresh chat for a new piece of work instead of adding it to a
long one.

<Choice id="why-the-chat-contradicts-itself" objective="concepts/explains-models/explains-generation"
  concepts={['context-window', 'token']}
  context="The lesson opens with a long chat about a report. The learner stated the audience and the page limit in the first message, and two hours later the assistant writes for a different audience at twice the length."
  title="Why did the answer change?"
  hint="Where was the first message when the model produced the latest answer? Count what has been sent to the model since then."
  options={[
    { text: 'The model changed its mind about the audience as the conversation went on.', why: 'A model has no opinion that persists between responses. Look at what it was given, not at what it wanted.' },
    { text: 'The first message was no longer among the tokens the model saw for this answer.', correct: true },
    { text: 'The model was trained on more engineering documents than finance documents.', why: 'Training data shapes what the model tends to write, but it does not explain why the model followed the instruction for two hours and then stopped.' },
    { text: 'Two hours is longer than the model can stay focused.', why: 'Time is not the limit. A model has no clock. Ask what else changed over those two hours.' },
  ]}>

After two hours of chat, the assistant that had been writing for the
finance team in two pages suddenly writes four pages for the engineering
team. Which explanation fits what you know about how a model produces an
answer?

</Choice>

<MultiChoice id="what-the-next-answer-sees" phase="review" objective="concepts/explains-models/explains-generation"
  concepts={['context-window']}
  context="The lesson explains that a model writes each answer from the tokens in its context window for that response, and that a long chat can push early messages out of it. The chat in this item has no memory features turned on."
  title="What can the next answer use?" hint="Which of these reach the window for this one response?"
  options={[
    { text: 'The file you attached earlier in this chat, which is still short.', correct: true },
    { text: 'The notes you keep in another tab of your browser.', why: 'The model only has what reaches its window. A tab it was never given does not exist for it.' },
    { text: 'The message you just sent.', correct: true },
    { text: 'What you told the same assistant in a chat yesterday.', why: 'With memory features off, yesterday\'s chat never enters today\'s window.' },
  ]}>

You are in a chat with an assistant that has no memory features. Which two
of these can the model use for its next answer?

</MultiChoice>

<Scenario id="the-budget-it-dropped" phase="review" objective="concepts/explains-models/explains-generation"
  concepts={['context-window', 'token']}
  context="In the first message of a long chat, the learner gave a budget of 5,000 euros. Many messages later the assistant proposes a plan that costs 9,000 euros."
  title="The budget from the first message" hint="Is the first message still among the tokens the model reads for this answer?"
  options={[
    { text: 'Tell the assistant it made a mistake and ask it to try harder this time.', consequence: 'The budget is still outside the window. The next plan misses it too, and the chat grows longer.' },
    { text: 'Ask the assistant which budget you gave it at the start of the chat.', consequence: 'The model can only answer from its window. It gives a number that sounds right, and you can\'t tell a guess from a memory.' },
    { text: 'Switch to a model with a bigger vocabulary so that it keeps numbers.', consequence: 'The vocabulary decides how text splits into tokens, not how many fit. The first message is still gone.' },
    { text: 'Restate the budget in your next message and ask for the plan again.', correct: true, consequence: 'The budget is back among the tokens the model reads, and the new plan stays under 5,000 euros.' },
  ]}>

Many messages ago you said the budget is 5,000 euros. The assistant now
proposes a plan for 9,000 euros. What do you do?

</Scenario>

<Exercise stretch="Paste the same page into a chat assistant and ask it how many tokens the page is. Compare its answer with the script, and decide which one you trust and why.">

Copy about one page of your own writing, an email or a report section, into
a file called `page.txt`. Run `python3 count_tokens.py page.txt`, then run
`python3 pages_per_window.py` with the token count it printed. Write down
the three numbers: characters, estimated tokens, and how many of your pages
fit in a 200,000-token window. With these numbers you can judge whether a
document fits before you paste it, and after how many turns a chat is full.

A good result is one line like "my page is 3,100 characters, about 750
tokens, and 266 pages fit in 200,000 tokens." Then answer this: how many
pages of instructions, pasted documents and earlier turns would your usual
chat sessions need before the first message falls out of the window?

</Exercise>

<Recap>

1. A context window is the maximum number of tokens the model takes into
   account for one response, and a page of English text is a few hundred
   tokens (@AEC-02).
2. The window holds the instructions, the conversation so far, anything
   pasted or attached, tool results and the answer being produced. Anything
   outside it does not exist for the model
   (@Academy ai-capabilities-and-limitations).
3. When a long chat contradicts itself, an early fact has left the window.
   Restate the fact in your next message instead of scolding the model.
4. A fuller window costs more per answer, because every token in it is
   processed for every response, and the model does not use every part of
   a full window equally well (@Academy ai-capabilities-and-limitations).

</Recap>
