import { Choice, MultiChoice, Match, Predict, Pitfall, Exercise, Recap } from '@components/lesson';

In the concepts course you named the turns of the agent loop: observe,
think, act. This lesson shows what the think step looks like when it
leaves the model as bytes. A model can't run a function. What it can do is
write a message that says "run this tool with these arguments", in a
format the vendor documents, and **function calling** is the name for that
mechanism (@Claude docs handle-tool-calls). The next lesson, "Building your
first agent", hides the format behind a fake model that returns
`{"tool": "get_weather", "args": {"city": "Lisbon"}}`. Here we look at the
real messages behind that fake reply, so that when you swap the fake model
for a real one you know which field to read.

Every example runs against a fixture: `fake_api`, a function that answers
the way the Claude API is documented to answer, so the output is the same
every time. The complete program is
`site/examples/building-agents/how-a-model-calls-a-tool/wire.py` in the
repository. Copy it to a folder of your own, and each step below is
`python3 wire.py <step>` in that folder. The messages
follow the Claude API. Other vendors use different field names for the
same four parts, and the mapping at the end of the lesson carries over.

## The request

A request to a stateless model API contains everything the model gets to
know (@Claude docs messages). For a tool-using agent that's a system
prompt, a list of tool definitions, and the message list so far. A tool
definition has a `name`, an `input_schema`, which is a JSON Schema object
that names each parameter and its type, and a `description`. The first two
are required. The description is optional, and it is the text the model
reads to decide when to call the tool, so leave it out only for a tool
whose name says everything (@Claude docs define-tools).

```python
TOOLS = [
    {
        "name": "get_weather",
        "description": "Current weather for a city.",
        "input_schema": {
            "type": "object",
            "properties": {
                "city": {"type": "string", "description": "City name, for example Lisbon."}
            },
            "required": ["city"],
        },
    },
]


def first_request(question):
    return {
        "model": "fake-model",
        "max_tokens": 200,
        "system": "You answer weather questions. Use the tool for live data.",
        "tools": TOOLS,
        "messages": [{"role": "user", "content": question}],
    }
```

The function itself isn't in the request. The model gets the
name, the description and the schema, and the function stays on your side
of the wire. The next lesson's fake model keeps the function and a one-line
description per tool and skips the schema. Here the schema tells the model
that `city` is a required string, and the API rejects a definition without
one.

## The reply that asks for a tool

The model reads the request and decides. When it wants the tool, the reply
has a `stop_reason` of `tool_use` and a `content` list with a `tool_use`
block in it (@Claude docs handle-tool-calls). The block has an `id` that's
unique to this call, the `name` of the tool, and an `input` object that
fits the tool's `input_schema`. A `text` block may come before it, and the
model often writes one sentence about what it is about to do.

<Predict id="first-reply" title="The first reply"
  answer={`{
    "id": "msg_01",
    "role": "assistant",
    "stop_reason": "tool_use",
    "content": [
      {
        "type": "text",
        "text": "I'll check the weather in Lisbon."
      },
      {
        "type": "tool_use",
        "id": "toolu_01",
        "name": "get_weather",
        "input": {
          "city": "Lisbon"
        }
      }
    ]
  }`} run="building-agents/how-a-model-calls-a-tool/first_reply.py">

Run the first step, and compare what you see with the output below.

```sh
python3 wire.py first_reply
```

</Predict>

The fixture's ids are short so you can read them. A real reply has a
longer id, a `toolu_` prefix followed by a run of letters and digits such
as `toolu_01Kx7mQp2rVdN8sLwe4Tzb9c` (@Claude docs handle-tool-calls).
Your loop never makes one up. It copies the id it was given.

Put the fake reply next to this one. `"tool": "get_weather"` is the `name`
field of the `tool_use` block, and `"args": {"city": "Lisbon"}` is its
`input`. The fake model had no id, because it only ever asked for one tool
at a time. It had no stop reason either. The loop tested for the `answer`
key instead. The real API tells you why it stopped in a field of
its own, and the `stop_reason` value is what your loop branches on
(@Claude docs handling-stop-reasons).

<Match id="fake-to-real" objective="building-agents/builds-agent-loop/defines-a-tool"
  concepts={['function-calling', 'tool']}
  context="The lesson compares a fake model that returns {'tool': 'get_weather', 'args': {'city': 'Lisbon'}} or {'answer': '...'} with a real API reply, which has a stop_reason field and a content list of typed blocks, one of which may be a tool_use block with id, name and input."
  title="Map the fake reply onto the real one" hint="Which field of the fake reply did the loop test to know whether to run a tool, and which field did it pass to the function?"
  options={['The `name` of the `tool_use` block', 'The `input` of the `tool_use` block', 'The `stop_reason` of the reply', 'The `id` of the `tool_use` block']}
  rows={[
    { statement: 'The `tool` key of the fake reply', option: 0, why: 'Which field says which function to look up?' },
    { statement: 'The `args` key of the fake reply', option: 1, why: 'Which field is passed to the function as keyword arguments?' },
    { statement: 'The presence or absence of the `answer` key', option: 2, why: 'What did the loop branch on, and which real field carries a value like end_turn or tool_use?' },
  ]}
  rationale="The fake model folded the stop decision into the reply's keys. The real API puts it in stop_reason, and adds an id the fake model never needed because it asked for one tool at a time.">
Each row names a part of the fake model's reply. Pick the field of the real API reply that plays the same role. One option isn't used.
</Match>

## The result goes back as a user message

Your loop finds the `tool_use` block, runs the function it names with the
`input` as arguments, and sends the result back. The Claude API has no
`tool` role. The result goes in a message with `role` `user`, as a
`tool_result` block whose `tool_use_id` quotes the id of the request it
answers (@Claude docs handle-tool-calls). The `content` is the result as a
string, or a list of content blocks when the result is more than text.

```python
def tool_result_message(reply):
    block = next(b for b in reply["content"] if b["type"] == "tool_use")
    result = get_weather(**block["input"])
    return {
        "role": "user",
        "content": [{"type": "tool_result", "tool_use_id": block["id"], "content": result}],
    }
```

<Predict id="tool-result" title="The tool result message"
  answer={`{
    "role": "user",
    "content": [
      {
        "type": "tool_result",
        "tool_use_id": "toolu_01",
        "content": "27°C, sun"
      }
    ]
  }`} run="building-agents/how-a-model-calls-a-tool/tool_result.py">

Run the second step, and compare what you see with the output below.

```sh
python3 wire.py tool_result
```

</Predict>

The next lesson's fake loop stores the same result as
`{"role": "tool", "content": "27°C, sun"}`. The real message says the same
thing in three more fields, and the one that matters is `tool_use_id`. When the model asks for two tools in one reply, the ids
are how it matches each result to the request it made. A tool that fails
sends the error text in `content` and sets `is_error` to `true`, and the
model reads the error and answers with it in mind
(@Claude docs handle-tool-calls).

<Choice id="where-the-result-goes" objective="building-agents/builds-agent-loop/defines-a-tool"
  concepts={['function-calling']}
  context="The lesson shows the message a loop sends back to the Claude API after running a tool the model asked for."
  title="Where does the tool result go?" hint="Which roles does the Claude API accept in a message list, and what links a result to the request it answers?"
  options={[
    { text: 'In a message with role `tool`, with the result text as its content.', why: 'Which roles does the messages list of the real API accept?' },
    { text: 'In a message with role `user`, as a `tool_result` block that names the `tool_use_id`.', correct: true },
    { text: 'In the `system` field of the next request, so the model sees it first.', why: 'The system prompt is a separate parameter with instructions for the whole conversation. Where do results of one call go?' },
  ]}>
The model asked for `get_weather` in a `tool_use` block with id `toolu_01`. Your loop ran the function and got `27°C, sun`. How does the result travel back?
</Choice>

## The second request repeats everything

The model doesn't keep state between calls (@Academy building-with-the-claude-api).
The second request repeats the system prompt, the tool definitions, and the
whole message list: the user's question, the assistant's reply with its
`tool_use` block, and the user message with the `tool_result`. The
`tool_result` message must come directly after the assistant message that
holds the `tool_use` block, with no message between them
(@Claude docs handle-tool-calls).

```python
def second_request(request, reply):
    messages = request["messages"] + [
        {"role": "assistant", "content": reply["content"]},
        tool_result_message(reply),
    ]
    return dict(request, messages=messages)
```

The reply to that request has `stop_reason` `end_turn` and a `text` block
with the answer (@Claude docs handling-stop-reasons). This is the fake
model's `{"answer": ...}` reply, and `end_turn` is where the fake model's
`answer` key went.

<Predict id="final-reply" title="The final reply"
  answer={`messages in the second request: 3
  {
    "id": "msg_02",
    "role": "assistant",
    "stop_reason": "end_turn",
    "content": [
      {
        "type": "text",
        "text": "It is 27°C, sun there."
      }
    ]
  }`} run="building-agents/how-a-model-calls-a-tool/final_reply.py">

Run the last step, and compare what you see with the output below.

```sh
python3 wire.py final_reply
```

</Predict>

One tool call took two requests and left three messages in the list. Each round of the loop sends
the list again with two more messages on the end, and the model's answer
reads as if it remembered, because you sent it the memory.

<Choice id="what-ended-the-turn" objective="building-agents/builds-agent-loop/defines-a-tool"
  concepts={['function-calling', 'stop-conditions']}
  context="The lesson shows two API replies: one with stop_reason tool_use and a tool_use block, and one with stop_reason end_turn and only a text block."
  title="How does the loop know it is done?" hint="What did the fake loop test, and which real field carries that decision?"
  options={[
    { text: 'The `content` list holds a `text` block with an answer in it.', why: 'The first reply had a text block too, before its tool_use block. What else did it have?' },
    { text: 'The message list has grown to three entries, so no tool round is left.', why: 'A model that needs two tool rounds sends five. What does the reply itself say?' },
    { text: 'The `stop_reason` of the reply is `end_turn`.', correct: true },
  ]}>
Your loop has sent the second request and received a reply. Which check tells it to return what the model wrote to the user rather than go round again?
</Choice>

<MultiChoice id="fields-that-must-match" phase="review" objective="building-agents/builds-agent-loop/defines-a-tool"
  concepts={['function-calling', 'tool']}
  context="The Claude API answers a tool request with a tool_use block (id, name, input) and expects the result back as a tool_result block in a user message."
  title="Which rules does the second request follow?" hint="Which value does the loop copy from the reply into the message it sends back, and where in the list does that message have to sit?"
  options={[
    { text: 'The `tool_use_id` of the result equals the `id` of the `tool_use` block.', correct: true },
    { text: 'The `tool_result` message comes right after the assistant message with the `tool_use` block.', correct: true },
    { text: 'The `content` of the result message repeats the `text` block the model wrote.', why: 'The model\'s own words are already in the history. What new information does the result carry?' },
    { text: 'The `stop_reason` of the result message is set to `end_turn`.', why: 'Which side of the conversation writes a stop reason?' },
  ]}>
The loop has run the function and builds the next request. Which of these does the vendor page require?
</MultiChoice>

<Choice id="one-tool-two-fields" phase="review" objective="building-agents/builds-agent-loop/defines-a-tool"
  concepts={['function-calling', 'tool']}
  context="A tool definition sent to the Claude API has a name, a description, and an input_schema. The function runs in the developer's own code."
  title="What reaches the model?" hint="Which parts of the fake model's TOOLS dict were text, and which part was code?"
  options={[
    { text: 'The function body, so the model can check what it does before calling it.', why: 'The model can\'t run code. What does it read instead to decide when to call the tool?' },
    { text: 'The name, the description and the input schema, and nothing that runs.', correct: true },
    { text: 'Only the name, because the model learns the arguments from the conversation.', why: 'How would the model know that city is required and is a string?' },
  ]}>
You define `get_weather` for a real model API. Which part of the tool is sent in the request?
</Choice>

<Pitfall title="Dropping the assistant message from the history">
A first loop often stores only the user's question and the tool result,
because those are the two things it wrote itself. The API then sees a
`tool_result` that doesn't follow the `tool_use` block it answers, and the
vendor page says a result block must come right after its request block,
so the request is rejected (@Claude docs handle-tool-calls). Rule: append the assistant reply as it
came, `content` list and all, before you append the result. The history
must show the request that the result answers.
</Pitfall>

## From one tool to many systems

The messages above are the whole mechanism, and for one tool and one
agent they're enough. The cost shows up when you have many agents and
many tools. Each agent has its own loop that turns a `tool_use` block
into a function call, and each tool (a calendar, a ticket system, a
database) has its own way of being called. With N agents and M tools you
write N x M adapters, and a change to one tool means a change in all the
agents that use it. This is the **N x M integration problem**
(@Academy introduction-to-model-context-protocol). A shared protocol
between agents and tools turns N x M into N plus M: each agent speaks the
protocol once, and each tool is wrapped once. The last lesson in this
course, on agent protocols, is about that protocol. For now, notice that
the `tool_use` and `tool_result` blocks are already a small protocol
between one model and your loop, and the rest of the course builds on
them.

<Exercise stretch="Change tool_result_message so the tool result reports an error with is_error set to true, and write the final reply the model would give. Check the field names against the vendor page on handling tool calls.">

Take the fake model's reply `{"tool": "get_weather", "args": {"city": "Lisbon"}}`
from the lesson "Building your first agent" and the tool's result
`27°C, sun`. Without running the fixture, write down the three real API
messages they become: the assistant reply with its `tool_use` block and an
id you choose, the user message with the `tool_result` that quotes that id,
and the final assistant reply. Then mark which field of the final reply
plays the role of the fake model's `answer` key.

A good result: the three messages have the roles `assistant`, `user`,
`assistant` in that order, the `tool_use_id` in the second matches the
`id` in the first, the `input` in the first is `{"city": "Lisbon"}`, and
you marked `stop_reason` with the value `end_turn` as the field that says
the model is done. Compare with the fixture's output when you are finished.
Reflect: which of the three messages did the fake loop write itself, and
which did it receive?

</Exercise>

<Recap>

1. A tool definition sends the model a name, a description, and an input
   schema, and the function stays in your code (@Claude docs define-tools).
2. A model asks for a tool with a `tool_use` block that has an id, a name
   and an input, and a `stop_reason` of `tool_use`. Your loop sends the
   result back in a `user` message as a `tool_result` block that quotes
   the id, and the whole history goes with it, because the model keeps no
   state (@Claude docs handle-tool-calls).
3. `end_turn` is where the fake model's `answer` key went, and the
   `stop_reason` field is what a real loop branches on
   (@Claude docs handling-stop-reasons).
4. Every agent that calls every tool its own way costs N x M adapters,
   which is the problem a shared protocol solves
   (@Academy introduction-to-model-context-protocol).

</Recap>
