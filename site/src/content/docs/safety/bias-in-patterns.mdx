import { Predict, Scenario, Pitfall, Exercise, Recap } from '@components/lesson';

In this lesson we read two shortlists that a ranker drew from the same
twelve candidate records, and each shortlist looks fair on its own. Then we
count who made the cut across both, and the count says something neither
shortlist says by itself. By the end you know why bias in a model's output
is a pattern you find by sampling and comparing, and how much checking a
task gets when its output affects people.

You need a terminal and `python3`. On a fresh Mac the first `python3`
command may open a dialog from Apple offering to install its developer
tools for the terminal (the Xcode Command Line Tools). Accept it, wait for
the install to finish, and run the command again. The ranker in this lesson
is a simulation, a short Python program written for the lesson, and the
records and names are made up. Its numbers say what a slant looks like in a
table of results, and they are not evidence about any real model. The real
evidence is in the papers cited in the second section.

## Each shortlist looks fine

A team has twelve candidate records for one role, and each record has the
same fields: years of experience, number of certificates, and months since
the last role. The team gives the records to a ranker, which scores each one
and shortlists the top six. Here is the shortlist it returned.

> 1. Elior Vance (experience 7, certificates 3, gap 0)
> 2. Fenwick Adair (experience 7, certificates 1, gap 3)
> 3. Dessa Morrow (experience 6, certificates 1, gap 2)
> 4. Oriel Sabani (experience 6, certificates 1, gap 2)
> 5. Corin Halvane (experience 5, certificates 0, gap 0)
> 6. Marisel Okonde (experience 4, certificates 2, gap 1)

Read it as the team would. The most experienced people are at the top, a
long gap costs a place, and a strong certificate count lifts a shorter
record. You would sign off on this list.

Now the same twelve records, ranked again, and this time each record
carries a different name. The names come from two lists of six, list A and
list B, both invented for this lesson. In the first run the even-numbered
records had list A names. In the second run the lists are swapped, so every
record has now carried one name from each list.

> 1. Quilla Tesfay (experience 7, certificates 3, gap 0)
> 2. Rashon Belka (experience 7, certificates 1, gap 3)
> 3. Corin Halvane (experience 6, certificates 1, gap 2)
> 4. Pell Varnava (experience 6, certificates 1, gap 2)
> 5. Arvel Dunmoor (experience 4, certificates 2, gap 1)
> 6. Brisa Tallent (experience 4, certificates 2, gap 2)

This one reads fine too. The same records lead, and the bottom two places
went to records with four years and two certificates, which is a defensible
call. List A is Arvel Dunmoor, Brisa Tallent, Corin Halvane, Dessa Morrow,
Elior Vance, and Fenwick Adair. List B is Marisel Okonde, Nadir Kesrou, Oriel
Sabani, Pell Varnava, Quilla Tesfay, and Rashon Belka. Before reading on,
count how many places on each shortlist went to a list A name.

## Count who made the cut

The ranker is `rank.py`, in the course repository under
`site/examples/safety/bias-in-patterns/`. Copy that directory or work in a
clone, and change into it. The program scores each record from its three
fields, ranks the twelve records, and prints the shortlist. Then it swaps the
name lists and does it again. With `--totals` it prints one line: how many
shortlist places went to each list, added up over both runs.

The swap is the point of the design. A ranker that reads only the fields
shortlists the same six records in both runs, because the fields did not
change. Each of those six records had a list A name in one run and a list B
name in the other. So the totals over both runs come out equal, six and six, whatever the
records say. If the totals are not equal, the name
changed the score, and the difference is the size of the slant.

<Predict id="predict-the-totals" objective="safety/verifies-output/calibrates-trust"
  concepts={['bias']}
  context="A simulated ranker shortlists six of twelve candidate records twice. In the first run the even-numbered records carry names from list A and the odd-numbered ones names from list B, and in the second run the lists are swapped, so each record has carried one name from each list. Under a ranker that reads only the record fields, each list would be shortlisted six times over both runs. The learner has both shortlists and both name lists on the page. With --totals the program prints one line: shortlisted over both runs: list A N, list B N."
  title="What are the totals?"
  hint="Count the list A names on each of the two shortlists above, then add. What would the two totals be if only the record fields counted?"
  answer="shortlisted over both runs: list A 7, list B 5"
  run="safety/bias-in-patterns/totals.py">
Before you run it, predict the line this prints. Write it the way the
program does, with both counts.

```sh
python3 rank.py --totals
```

</Predict>

Seven and five. You could not have read that from one shortlist. In the first run list A took
four of six places and in the second three of six, and a reader who saw
either one saw a plausible split. Only the pair, with the names swapped and
the counts added, shows that two shortlist places moved with the name.
Run the program without the flag to see both rankings with their scores.

<Predict id="both-runs" title="Both rankings, with scores"
  answer={`Run 1: list A names on the even records
   1. shortlist  score 28  Elior Vance     list A  (experience 7, certificates 3, gap 0)
   2. shortlist  score 21  Fenwick Adair   list A  (experience 7, certificates 1, gap 3)
   3. shortlist  score 19  Dessa Morrow    list A  (experience 6, certificates 1, gap 2)
   4. shortlist  score 18  Oriel Sabani    list B  (experience 6, certificates 1, gap 2)
   5. shortlist  score 16  Corin Halvane   list A  (experience 5, certificates 0, gap 0)
   6. shortlist  score 15  Marisel Okonde  list B  (experience 4, certificates 2, gap 1)
   7.            score 14  Arvel Dunmoor   list A  (experience 4, certificates 2, gap 3)
   8.            score 14  Nadir Kesrou    list B  (experience 4, certificates 2, gap 2)
   9.            score 13  Brisa Tallent   list A  (experience 4, certificates 1, gap 2)
  10.            score 12  Pell Varnava    list B  (experience 3, certificates 2, gap 1)
  11.            score  8  Quilla Tesfay   list B  (experience 2, certificates 2, gap 2)
  12.            score  7  Rashon Belka    list B  (experience 3, certificates 0, gap 2)
  shortlisted: list A 4, list B 2

Run 2: the same records, names swapped
   1. shortlist  score 27  Quilla Tesfay   list B  (experience 7, certificates 3, gap 0)
   2. shortlist  score 20  Rashon Belka    list B  (experience 7, certificates 1, gap 3)
   3. shortlist  score 19  Corin Halvane   list A  (experience 6, certificates 1, gap 2)
   4. shortlist  score 18  Pell Varnava    list B  (experience 6, certificates 1, gap 2)
   5. shortlist  score 16  Arvel Dunmoor   list A  (experience 4, certificates 2, gap 1)
   6. shortlist  score 15  Brisa Tallent   list A  (experience 4, certificates 2, gap 2)
   7.            score 15  Oriel Sabani    list B  (experience 5, certificates 0, gap 0)
   8.            score 13  Marisel Okonde  list B  (experience 4, certificates 2, gap 3)
   9.            score 13  Dessa Morrow    list A  (experience 3, certificates 2, gap 1)
  10.            score 12  Nadir Kesrou    list B  (experience 4, certificates 1, gap 2)
  11.            score  9  Elior Vance     list A  (experience 2, certificates 2, gap 2)
  12.            score  8  Fenwick Adair   list A  (experience 3, certificates 0, gap 2)
  shortlisted: list A 3, list B 3

shortlisted over both runs: list A 7, list B 5`}
  run="safety/bias-in-patterns/both_runs.py">
Run the ranker in full, and compare what you see with the output below.

```sh
python3 rank.py
```

</Predict>

Look at the record with six years, one certificate and a two-month gap.
In the first run it has the name Dessa Morrow from list A and scores 19,
third place. In the second run it has the name Pell Varnava from list B and
scores 18, fourth place. Same record, one point less, one place lower. The
simulation adds one point to every list A name, and that point moved two
records across the shortlist line over the two runs. A real model has no
such line of code to read. The effect is spread through its weights, and
this program exists so that you can see what the effect does to a table of
results without reading any weights.

## Where the slant comes from

**Bias** in a model is a systematic slant in its output that it inherited
from its training data and its tuning. The model learned which words
follow which from a large body of text, and in that text some names
appear more often next to senior job titles and some dialects more often in
formal documents. A model
predicting the most likely continuation reproduces those frequencies, so
the same record reads as a slightly better fit under one name than under
another (@Academy ai-capabilities-and-limitations). Tuning after training
can reduce a slant or add a new one, because the people and the rubrics
that grade the model's answers have preferences of their own.

This is measured, and the measurement uses the same swap as the fixture.
Bertrand and Mullainathan sent nearly five thousand fictitious resumes to
real job advertisements, identical except for the name, and found that
resumes with white-sounding names got about fifty percent more callbacks
than the same resumes with African-American-sounding names (@Bertrand 2004).
Wilson and Caliskan ran the method against language models used for
resume retrieval, with over five hundred resumes and job descriptions across
nine occupations, and found that the models favored white-associated names
in 85.1 percent of the tests, favored female-associated names in 11.1
percent, and in some settings put Black male names at a disadvantage in
every test (@Wilson 2024). The models were not told anyone's race or gender.
The name was enough, as it was for the employers in 2004.

Both studies needed thousands of comparisons to find this. One output of
a slanted model is a plausible answer, the same as one output of a fair
one. The slant is a shift in the odds, and odds show up only when you
have many draws to count. That is what makes bias different from the
invented fact in the previous lesson: a hallucination can be caught in the
sentence it appears in, and a slant cannot.

<Pitfall title="It ranked one candidate the way I would have">
A manager tests a screening assistant by giving it three applications she
knows well and asks it to rank them. The order matches hers and the reasons
read well, and she rolls it out for the next hundred. Each of the three
outputs was reasonable, and three outputs can't
show a slant of one point in a score, which is all it takes to move a record
across the shortlist line. Rule: a fairness check is a comparison of counts
across groups over many records, and one agreed answer, or three, is
evidence about those answers and nothing else.
</Pitfall>

## How much checking a screening task gets

The lesson [What an invented fact looks like](/safety/spotting-hallucination/)
sized the checking to the stakes of a claim. The same rule applies here,
and the stakes of a task that affects people are set by who it affects. A
model that drafts your meeting notes affects you, and you catch its
mistakes as you read. A model that screens applications, triages support
tickets by customer, or scores essays affects people who never see the
output and can't correct it. That is the high-stakes end, and the checking
it gets is different in kind from reading a few outputs.

The check that fits is the one the fixture did. Name the groups the task
could treat differently. Take a sample of records that is large enough for
a pattern to show, and the fixture shows that a dozen is barely enough for
a slant of one point. Run the same records under swapped markers of the
group, a name or a dialect or a postcode, and count the outcomes per group.
Give the counts to someone who can judge the records and who has the
standing to stop the rollout. The team trusts the tool for that task after
the comparison comes back level, and keeps repeating the comparison, because
the model changes and the applicants change.

<Scenario id="screening-three-hundred" objective="safety/verifies-output/calibrates-trust"
  concepts={['bias']}
  context="A team wants a chat assistant to shortlist twenty interview candidates from three hundred internship applications. The applicants will never see the output. The learner decides how much checking the task gets before the tool is trusted for it."
  title="The internship shortlist"
  hint="Who is affected by a wrong shortlist, and can they correct it? What kind of check can show a shift in the odds?"
  options={[
    { text: 'Ask the assistant to explain its reasons for each shortlisted applicant, and read them.', consequence: 'Every reason reads well, because the model writes fluent reasons for whichever twenty it picked. The applicants it did not pick are not in the list you read, so a slant against them is invisible.' },
    { text: 'Have a recruiter spot-check five of the twenty and go ahead if those five look right.', consequence: 'The five look right. Five agreed picks say nothing about the two hundred and eighty who were left out, or about who was left out more often.' },
    { text: 'Before use, run a sample of the applications with names swapped across the groups the team can name, count the shortlist rate per group, and have the recruiting lead read the counts and decide whether the tool runs and how the result is reviewed.', correct: true, consequence: 'The comparison shows whether a name moved the score, and a person with standing decides on that evidence. If the counts come back level, the tool is trusted for this task, and the check is repeated when the model or the applicant pool changes.' },
    { text: 'Rely on the vendor statement that the model was tested for fairness before release.', consequence: 'The vendor tested its model on its data and its prompts. Your applications, your job description and your prompt are a different setting, and the papers on this found that the slant changes with the setting.' },
  ]}>

A team wants to use a chat assistant to shortlist twenty interview
candidates from three hundred internship applications. Time is short. What
checking does the task get before the team trusts the tool for it?

</Scenario>

<Exercise stretch="Change LIST_A_BONUS in rank.py to 0 and run it again. Then set it to 1 and change one record so the totals come out equal anyway. What does that tell you about how many records a check needs?">

Pick one task at your work where a model's output affects people who don't
see it: a shortlist, a triage queue, a scoring, a decision about who gets
contacted first. Write down which groups the output could treat differently
and what marker in the input signals each group. Then write down how many
records you would need to compare before you would trust a pattern, and
who reads the counts and has the standing to stop the rollout. Fifteen
minutes is enough.

A good result names at least two groups, one input marker per group, a
sample size with a reason attached, and a named person or role who decides.
Is that person the one who wants the tool, or someone who can say no to
it?

</Exercise>

<Recap>

1. Bias is a systematic slant a model inherited from its training data and
   tuning. It shifts the odds, and one output of a slanted model looks
   as reasonable as one output of a fair one
   (@Academy ai-capabilities-and-limitations).
2. The slant becomes visible in totals: the same records under swapped
   group markers, the outcomes counted per group, compared. A fair ranker
   gives equal totals by construction, so a difference is the slant.
3. This has been measured in employers and in models with the same
   swapped-name method, and the models favored some names for the same
   record without being told anything about the person (@Bertrand 2004)
   (@Wilson 2024).
4. A task whose output affects people who can't correct it is high stakes,
   and its check is a sampled comparison across groups, read by someone who
   can judge the records and stop the rollout, repeated as the model and
   the inputs change.

</Recap>
