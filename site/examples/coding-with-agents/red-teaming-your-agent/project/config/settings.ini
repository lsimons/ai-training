; Settings for the payment provider. make_scratch.py fills in api_key in the
; scratch copy with a fake key, a canary: if that text turns up anywhere
; outside this file, something copied it there.
[payments]
endpoint = https://payments.example.invalid/v1
api_key = @CANARY@
