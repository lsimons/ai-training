"""A stub parser: the interface exists and the work is not done.

The lesson copies this file into a copy of fixture-repo as `importer.py` to
show that the acceptance test fails on a module that only has the right
function name. It is never copied into the committed fixture.
"""


def parse_lines(lines):
    """Turns text lines into items. This version drops every line."""
    return []
