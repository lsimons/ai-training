# Agent instructions for invoices

- `invoices.py` holds the invoice rules.
- `requirements.lock` pins every dependency to one version.

## Working with agents

- Be careful with dependencies.
- Don't lose other people's work.
- Say when an agent helped with a commit.
- Agents never edit the database migrations in `migrations/`.
