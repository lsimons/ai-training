# Agent instructions

- `shipping.py` holds the price rule, and `test_shipping.py` tests it.
- `README.md` documents the module. Its `>>>` examples are checked.
- Run `python3 check.py` after each change, and report its last line.
