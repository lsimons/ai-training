# Lesson example fixtures: the same rules as the repo (extend), but
# targeting Python 3.9 so nothing here needs a newer interpreter than a
# learner is likely to have. Issue #29 is the CI run on 3.9 itself. The PTH
# exemption for this directory is in the root pyproject.toml.
extend = "../../pyproject.toml"
target-version = "py39"
