# Handoff: time zone fix

Written for the lesson as an example of a handoff that lists files and
leaves out the decisions. It is not the output of a model.

Changed files:

- `test_reports.py`

Files to read first:

- `reports.py`, where `format_ts()` formats the times
- `test_reports.py`

Tests: 4 passed.

Next: finish the time zone fix.
