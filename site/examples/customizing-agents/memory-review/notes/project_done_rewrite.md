---
name: done is being rewritten
description: Leave done() alone; the rewrite is on the done-v2 branch
type: project
---

The `done` command is being rewritten on the done-v2 branch. Do not change
`done()` in todo.py on any other branch. If a task needs it, tell the user
the fix belongs in done-v2 and wait for that branch.
