# notes

A notes command in one file. This package is the fixture for the lesson
"Writing your first skill" in the AI training site. The release procedure
is in `.claude/skills/release/SKILL.md`, and `release_check.py` is the
check that procedure ends with.

Run the tests with `python3 -m unittest -q` and the check with
`python3 release_check.py`.
