# One answer to the exercise

For the lesson "Turning repeated work into something the agent keeps". The
course wrote these files, and no model produced them.

- `instructions-after.md`: the instructions file after the sort and the deletion
- `SKILL.md`: the skill for the regeneration, at `.claude/skills/regenerate-api-client/SKILL.md` in the project
- `billing-call.md`: the example, at `docs/examples/billing-call.md` in the project
- `hook-message.txt`: what the hook writes to standard error when it blocks an edit under `src/api/generated/`
