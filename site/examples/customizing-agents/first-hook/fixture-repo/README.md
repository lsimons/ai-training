# shelf-notes

A tiny project for the lesson "Writing a hook that blocks a mistake".

The lesson renames `package.sample.json` to `package.json` and
`package-lock.sample.json` to `package-lock.json` in your copy. The course
repository keeps them under other names so that no dependency tool reads them
as a real project.
