# Session record, 2026-09-23, wave 6

The first wave run by the meta-orchestration dispatcher
(`docs/agents/meta-orchestration.md`), with a disposable wave lead in place of
the session coordinator. Six lesson issues, one per area, picked by
`mise run next-wave -- --size 6`: #127 (concepts), #114 (safety), #136
(using-agents), #144 (coding-with-agents), #179 (customizing-agents) and #191
(building-agents). Six branches, one wave branch `wave/6-second-lessons`, one
pull request.

## Counts

Six builders, six reviewers, twelve review passes. Every first review said
needs changes (two of them with Blocking findings), every re-check approved,
zero merged with an open finding. Four of the six branches conflicted with an
earlier one in the bibliography or the S02 source table, all add/add, and the
lead resolved them by keeping every line. One e2e spec assumed eight live
Concepts lessons and the lead changed it to nine.

## What review caught

- A lesson whose own template broke the rule it taught (a line goes into the
  template after two corrections; the template held one).
- A Scenario option the page's own rule made defensible (typing twelve
  numbers instead of attaching the file), and a second one in another lesson
  (a copyleft library offered as a dependency the license didn't allow).
- Two vendor limits that disagree between Anthropic's Vision doc and its Help
  Center (10 MB against 500 MB per file), shipped as one figure.
- A cropping recommendation attributed to the Vision doc that says the
  opposite.
- Two lessons with near-verbatim vendor sentences under an S02 row that
  promises "no text adapted".
- Three false claims about Claude Code permission rules: "don't ask again"
  saving an Edit approval, nothing after a `*` limiting a rule, and a fresh
  clone starting from an empty allowlist while `~/.claude/settings.json`
  applies everywhere.
- A pitfall whose slow call was the last one asked, so the mix-up it described
  couldn't happen, and an off-by-one in its timing arithmetic.
- The `is_error` rule cited as a guarantee that the model reissues a dependent
  call, where the docs describe a tendency.
- A fixture that read a `.env` file `site/.gitignore` had kept out of the
  tree, so `mise run examples` passed only in the builder's worktree.
- S02 source-table rows wider than the column on two branches, which mdformat
  rewrites into a 19-line diff and `mise run lint` then fails.
- `ICMJE` and `Stenberg`, both real, filed under invented names in the word
  list.
- An unsourced "read what your agent sends" and an unsourced "within a
  minute" for key revocation.

## What the builder prompt should have said

- The sidebar in `astro.config.mjs` is generated from the data tree now, so
  the "manual sidebar" collision note is stale. Every builder checked and
  left the file alone, but the note costs each one a read.
- `mise run lint` (the prek hooks, mdformat included) is part of `ci` and not
  in the list of individual tasks the prompt names. Two S02 rows shipped too
  wide for the table because nobody ran it. Name it, and say a new S02 row
  must fit the existing column widths.
- Fixtures must read only files `git ls-files` lists. `site/.gitignore`
  ignores `.env`, so a fixture that needs one commits a sample under another
  name and copies it at run time.
- Two branches adding the same `Claude Code <page>` bibliography key is
  likely in any wave with two agent-tooling lessons. Say the key must be
  byte-identical (entry and S02 row) so the lead can drop one copy.
- Vendor limits should be quoted only where two vendor pages agree, with
  `sources-checked` and `review-by` in the plan.
