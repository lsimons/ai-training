# Meta session, 2026-09-24 (code)

A dispatcher run over the open `code` issues, started 2026-09-24 in the
morning. The picker handles only lesson and content issues, so the
dispatcher orders the waves by hand: bugs first, small improvements second,
bigger changes last, and at most one issue per collision group in a wave
where it can.

## Arguments

- `/wave 10`, code issues, filing on (the default filing paragraph).
- Out of scope: #238, #237 and #233 (mostly lesson content, for a content
  run), #244 and #245 (`ready-for-human`).

## Remaining `--only`

67 221 231 274 310 317 318 319 320 321 322 330 331

## Wave plan

- Wave 9: #252 and #234 (bugs), #84, #103, #102, #56, #112, #101, #228, #68.
- Wave 10: #98 (checkpoint reader, before #232), #58, #85, #220, #236, #69
  (after #68), #279, #280, #277, and #282 as the nits row.
- Wave 11: #286 (bug), #111 (after #85), #71 (unblocks #70), #275, #276,
  #278, #290, #287, #291, and #292 as the nits row.
- Wave 12: #281, #285, #288 (decided by the maintainer on 2026-09-24),
  #289, #70 (after #71), #294, #295, #296, and #297 as the nits row.
- Wave 13: #232 (after #98 and #234), #299, and #300 as the nits row.
- Wave 14: #67 (collides with #232 on the progress record and the review
  page).
- Wave 15: #221 (touches ten components, so it runs alone).
- Wave 16: the S12 spec of #231 only. The maintainer chose on 2026-09-24
  to review the spec before any build step, so this wave's pull request
  stays open and the run stops there. The four build steps run in a later
  session.

Wave 9 filed #275 to #282. The `ready-for-agent` ones joined the plan
above. #281 is `ready-for-human`. The maintainer decided #278 on
2026-09-24, so it joined wave 11, and agreed to add #71 (a `content`
issue) so #70 can run. Wave 10 filed #284 to #292. #284 is lesson content
blocked by #237, so it stays with a content run. #285 and #288 are
`ready-for-human`.

The maintainer decided #281, #285 and #288 on 2026-09-24 (comments on
each issue), and they joined wave 12. The maintainer also set the
`robots.txt` stance for #231 (allow every crawler). Wave 11 filed #294 to #297,
and all four joined wave 12.

Wave 12 filed #299 and #300, and both joined wave 13.

Wave 13 filed #302 (nits) and #303 to #308 (review alternates, one
`content` issue per area, for a content run).

## Parked

None yet.

## Waves

### Wave 9

```text
WAVE 9 merged
PR: #283
Merged issues: #252 #234 #84 #103 #102 #56 #112 #101 #228 #68
Left out: none
For the maintainer: nothing blocks. Four calls, stated in the PR body:
(1) #112: the footer date is Starlight's git-based date per page, with
fetch-depth 0 in deploy.yml. (2) #68: fenced code and code spans are
skipped when components and links are read, and the remark MDX parser
stays with #231. (3) #102: .md files under site/ always run e2e.
(4) #234: fixed-position stays "every item". Decide #278 and #281. The
global build skill (outside the repo) doesn't run mise run prose-sync in
its setup.
Filed: #275 #276 #277 #278 #279 #280 #281 #282
Follow-ups: none
```

Nineteen review passes. Eight first reviews said needs changes, and every
re-check approved. `mise run ci` passed on the wave branch on the first
run, and CI and the deploy on `main` passed.

### Wave 10

```text
WAVE 10 merged
PR: #293
Merged issues: #98 #58 #85 #220 #236 #69 #279 #280 #277 #282
Left out: none
For the maintainer: nothing blocks. Calls, stated in the PR body:
(1) #220 stacks on #98 and reads Predict through #98's MDX reader.
(2) #236 also bans py, zsh and console fences, as aliases of the listed
tags. (3) #69's tutor bootstrap takes its fetch base from the lesson URL,
so local runs work, with GitHub Pages as the fallback. (4) #58 drops the
playwright package, which @playwright/test still pins exactly in
bun.lock. (5) #98 rejects string props like revision="2". Decide #285
and #288. CI and the deploy on main (3aafefb) passed.
Filed: #284 #285 #286 #287 #288 #289 #290 #291 #292
Follow-ups: none
```

### Wave 11

```text
WAVE 11 merged
PR: #298
Merged issues: #286 #111 #71 #275 #276 #278 #290 #287 #291 #292
Left out: none
For the maintainer: none. Calls noted in the PR: spec S08 "Recommended
flags" drops the nonexistent --safe-mode. covered-by is a lesson YAML
field documented in S11, and its Playwright step was skipped because no
lesson has the field. Whether mise run data checks the covered-by href
went onto #288.
Filed: #294 #295 #296 #297
Follow-ups: none
```

### Wave 12

```text
WAVE 12 merged
PR: #301
Merged issues: #281 #285 #288 #289 #70 #294 #295 #296 #297
Left out: none
For the maintainer: none. #294 puts the mobile skip tip under Starlight's
fixed mobile bar, not inside the dropdown. #285's test also reads site
from astro.config.mjs. No live lesson failed the stricter #281 rule
(highest share 2 of 4), so no lesson page changed.
Filed: #299 #300
Follow-ups: none
```

### Wave 13

```text
WAVE 13 merged
PR: #309
Merged issues: #232 #299 #300
Left out: none
For the maintainer: none. #232 keeps alternates matched by objective.
One review session never asks the same alternate twice. An alternate's
own revision doesn't reset the review item, which S03 and S05 now state.
The progress record stays at v2 with an additive practice map and an
optional served id, so #67 can build on it. CI on main green.
Filed: #302 #303 #304 #305 #306 #307 #308
Follow-ups: none
```

## Paused

The maintainer stopped the run after wave 13 on 2026-09-24. Nothing is in
flight, no branch holds unmerged work for this run, and no worktree is
left. The next `/wave` session picks up the queue in this order:

- Wave 14: #67 (habit layer, on top of the v2 progress record from #232),
  with #302 as the nits row.
- Wave 15: #221 (inline scripts out of ten components), run alone.
- Wave 16: the S12 spec of #231 only, left open for the maintainer's
  review. The four build steps follow in a later wave after approval.
- Content run: #303 to #308 (review alternates per area), #237, #238,
  #284 and #233.

## Resumed

The maintainer resumed the run on 2026-09-24 in the evening, while a
lessons run (`2026-09-24-meta-lessons.md`, wave 16 onward) works on
another machine. Wave 14, 15 and 16 of the plan above never ran under
those numbers, because the lessons run used them.

Rules for running next to the lessons run:

- Code waves are named `c1`, `c2` and so on, on branches `wave/c<k>-code`,
  with the record in `docs/agents/sessions/<date>-wave-c<k>.md`. The
  lessons dispatcher counts numeric waves and resumes any `wave/<n>-*`
  branch without a record, so a numeric code wave would look like one of
  its own.
- This dispatcher never edits `2026-09-24-meta-lessons.md` or the wave
  lead template. A code wave's `Add to collision notes` lines are kept
  below and go into the template once the lessons run stops. Until then
  they're added to the filled prompt of each later code wave.
- Each issue is assigned when its wave starts, so the lessons picker, which
  skips assigned issues, never takes it.
- A code lead leaves lesson pages, lesson and plan YAML, the bibliography,
  `cspell-words.txt` and the S02 table alone unless its issue names them,
  rebases the wave branch on `origin/main` and reruns `mise run ci`
  right before the merge, and names any stricter check in the PR body.

The reconciliation of the same evening filed #317 to #331 and moved #310
to `ready-for-agent`. #323 waits for 2026-09-27, and #324 to #329 are
`content` issues for a content run.

Plan:

- Wave c1: #67 (habit layer), #274 (review line to the footer), #317
  (wrapped citations), #319, #320, #321 and #322.
- Wave c2: #318 (e2e literals, after #67 changes the progress seed), #330
  (after #320), #331 (after #321) and #310 (after #317).
- Wave c3: #221, run alone, after #67 and #274 touch the same components.
- Wave c4: the S12 spec of #231 only, left open for the maintainer's
  review, and the run stops there.

In flight: wave c1, branch wave/c1-code, issues #67 #274 #317 #319 #320 #321 #322

## Pending collision notes

None yet.
