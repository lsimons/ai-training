# Session record, 2026-09-23, wave 7

The second wave run by the meta-orchestration dispatcher
(`docs/agents/meta-orchestration.md`). Six lesson issues, one per area, picked
by `mise run next-wave -- --size 6`: #134 (concepts), #116 (safety), #148
(using-agents), #153 (coding-with-agents), #181 (customizing-agents) and #194
(building-agents). Six branches, one wave branch `wave/7-third-lessons`, one
pull request.

## Counts

Six builders, six reviewers, twelve review passes. Four first reviews said
needs changes and two said approve with Minors, every branch got one fix
commit, every re-check approved, and three branches took one more one-line
commit for a factual nit the re-check named (#148, #194, #116). Zero merged
with an open finding. One branch (#116) conflicted with an earlier one in the
word list and the bibliography, both add/add, and the lead resolved them by
keeping every line. One e2e spec assumed nine live Concepts lessons and the
lead changed it to ten. Follow-ups filed: #247, #248, #249 (nits left open on
merged branches) and #250 (an audit of live lessons whose plan emptied
`after`).

## What review caught

- Markdown headings inside a `<Response>` block that compiled to real page
  headings and showed up in the "On this page" sidebar.
- A Repair item whose gap (no responsible person named) the shown text had
  already filled, so the failure it described couldn't happen.
- A fixture whose graded result rested on an index tie-break the page never
  mentioned, so reordering the records gave an even split.
- "Invented names with no real-world group" that were all Anglo surnames on
  one list and names with real ethnic signal on the other, so the fixture
  reproduced the papers' slant while calling itself neutral.
- A page that stated the cause of sycophancy as settled where the paper says
  "likely driven in part".
- An exercise answer that named the committed settings file as the deciding
  layer where the page's own model, and the fixture, named the local one.
- A Predict whose answer needed layer names the rendered page never showed.
- Loop arithmetic ("forty steps, thirty-eight wasted") that the fixture
  contradicted: the character budget fired after round seven.
- A vendor sentence copied word for word under an S02 row that promises no
  text adapted.
- A reset recipe (`git checkout -- .`) for a directory the page told the
  learner to copy, so it had no `.git`.
- Two Sort items that described behavior the fixture already had.
- Three plan files with `assumes: []` on pages that depended on a live lesson,
  and one whose builder emptied `after` against spec S11 step 3.
- A probability claim ("half the time") that was about a quarter.

## What the builder prompt should have said

- The issue bodies were written for the old data layout (frontmatter,
  `status: live`, `site/src/data/courses/`). The lead's prompt said so and
  pointed at `writing-a-lesson.md`, and no builder tripped on it. Keep that
  paragraph until the issue bodies are corrected.
- Keep `after` as it was when the lesson goes live (spec S11 step 3). One
  builder emptied it, and earlier waves did the same on several lessons
  (#250).
- A page that says "as the last lesson taught" needs an `assumes` entry for
  it, even when the plan file arrived with `assumes: []`.
- Content inside `<Response>` that looks like Markdown (headings, lists)
  must be fenced, or it renders as page structure.
- Every number on a page that a fixture can produce must be pasted from the
  fixture's output, including numbers in prose and not only in `answer`
  strings. Three of the six branches had one arithmetic or count claim the
  fixture contradicted.
- Invented names must be checked for group signal, not only for existence.
- Cheap nits from a re-check can go back as one more one-line commit
  without another review round; the lead reads the diff. Three branches
  took that path and it cost minutes.
