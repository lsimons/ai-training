# Session record, 2026-09-24, wave 14

A lessons wave run by the meta-orchestration dispatcher
(`docs/agents/meta-orchestration.md`), with one or two planned lessons per
course going live: `concepts/what-tokens-cost` (#137), `concepts/agent-loop`
(#140), `safety/when-the-tool-is-usually-right` (#118),
`safety/tracing-a-planted-instruction` (#132), `using-agents/when-to-stop`
(#156), `using-agents/trying-two-models` (#161),
`coding-with-agents/decomposing-the-work` (#150),
`coding-with-agents/spec-driven-increments` (#155),
`customizing-agents/subagents` (#187) and `building-agents/loop-on-an-sdk`
(#196). The nits row was #302 and #256 on one branch. Eleven branches, one
wave branch `wave/14-lessons`, one pull request.

## Counts

Eleven builders, eleven reviewers, twenty-two review passes. Every first
review said needs changes, and every re-check approved. The blocking
findings were near-verbatim vendor sentences in the subagents lesson, and a
split citation in the nits branch that the built page showed as literal
text. The subagents re-check left two cheap nits, which went back as one
more commit that the lead read instead of running another review pass. Zero
merged with an open finding.

The first six builders ran at once, then the other five. The integration
rebases hit add/add conflicts only in `site/src/data/bibliography.yaml`
(the tokens lesson after the safety lesson, the SDK lesson after the tokens
lesson, the subagents lesson after the SDK lesson) and in the S02 source
table, where the subagents and SDK lessons both added rows after
`Claude Code best practices`. The lead kept every line, in course order.
Both concepts branches added the same `Bram` line to `cspell-words.txt`
at the same place, and git merged it cleanly. The wave's `mise run ci`
failed one e2e test, which pinned `concepts/straight-answer` as the last
live concepts lesson. The lead changed it to read the last live concepts
lesson from the data tree, as the rest of the suite does since issue #242.

The lead made these calls:

- Foundations fixtures. A foundations page may not contain `<Predict run=...>`
  (spec S03 "Foundations audience"), so #137, #140 and #132 each pasted
  fixture output into `text` fences and added a pytest file under `tests/`
  that checked the page against the fixture. The #137 review called that a
  new CI mechanism that would decide ahead of #237, which decides how a hidden fixture
  backs a foundations page. Those tests were deleted, the fixtures stay
  under `site/examples/` as the source of the pasted output, and #311 wires
  them in once #237 is merged.
- #140: the plan title "Run a tiny agent" contradicted a page where the
  learner runs nothing, so the title is now "Watch a tiny agent work".
- #132: the review found "pauses before acting by default" unsupported by
  the cited page. The same sentence was in the live `safety/agent-risk`, so
  the #132 branch fixed both.
- #132 keeps a graded `Predict` without `run`. With the test gone, its "not
  run in CI" notice is accurate, and #311 records the wording for #237.
- #150 keeps its plan's `assumes adjusts-mid-task`, which the reviewer
  found unused on the page, because the plan chose it.
- #155 linked the live `plan-then-test` page to the new lesson in its
  review commit.

## What review caught

- #187: two sentences near-verbatim from the vendor sub-agents page, and a
  graded key that said `disallowedTools: Edit, Write` means "cannot change
  files" while the page itself showed Bash editing files. Also an exercise
  whose `git status` check could never show "nothing changed", a Pitfall
  that had `acceptEdits` run a commit without a prompt, and "a subagent
  starts empty" against the vendor list of what it starts with.
- #118: the NTSB Asiana report cited for a contributing factor that is a
  finding, and a 6 to 11 percent figure read as a share of clinicians where
  the review counts cases.
- #196: a near-verbatim example prompt from the vendor agent-loop page, and
  a graded prediction whose program printed a second line the page never
  mentioned.
- #137: the wrong reason for output tokens costing more, three different
  call volumes on one page, and "example" prices that were exactly the
  current list prices.
- #150, #155 and #140: an Order or Sort item with a second defensible
  answer. In #140 the Sort was solvable from the `model:` and `loop:`
  prefixes alone.
- #150: a claim that the previous lesson wrote the import criteria, where
  that lesson's feature is due dates.
- #155: an `assumes: []` on a page that depends on the success criteria
  another live lesson teaches, and a spec quote from the wrong bullet.
- #156: a log that contradicted the live `adjusting-the-brief` story, and a
  model answer that rejected a defensible round.
- #161: a sentence that named a failure mode the linked section doesn't
  list.
- #132: a Match that re-asked the `agent-risk` Match with the same rows.
- Nits branch: a rewrap that split `(@EC AI Act)` across two lines, so the
  built page lost one of its 21 citation links.

## What the builder prompt should have said

- A foundations lesson backed by a fixture pastes the output into a `text`
  fence and doesn't add a test or other CI check of its own until #237 is
  merged. The builders of #137, #140 and #132 each wrote the same pytest
  check, and each removed it in review.
- A rewrap of lesson prose keeps every `(@key)` citation on one line, and
  the builder compares the built citation count with `main`.
- A plan title that says the learner runs something is checked against a
  foundations page where the learner runs nothing.

## Filed

- #310 Build check rejects an unresolved (@key) citation in rendered HTML
  (filed by the nits reviewer, `ready-for-human`)
- #311 Wire the wave 14 foundations fixtures into the #237 proof mechanism
- #312 Cosmetic nits left open on wave 14 branches

Follow-ups: none.
