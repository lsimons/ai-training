# Meta session, 2026-09-24

An unattended overnight run of the meta-orchestration dispatcher
(`docs/agents/meta-orchestration.md`), started 2026-09-24 in the evening.
No issues are filed during this run, except the follow-ups that issue #259
asks for. Follow-ups from the waves and the single pull requests are listed under "Follow-ups for the
maintainer" below, for the maintainer to file.

## Arguments

- Whitelist: the issues listed under "Remaining whitelist" at the start,
  plus the `ready-for-agent` children of #259.
- Waves run as `/wave 8 --only <numbers> --no-filing`, lessons first, then
  content. Code issues run one pull request at a time.
- Standing approval for a green wave or a green single pull request. A pull
  request that creates or rewrites a spec under `docs/spec/` is left open for
  the maintainer.
- Maintainer instruction during the run: the LaTeX toolchain (`lualatex` via
  TinyTeX) on this machine is broken. Builders render Quarto decks with
  `--to revealjs` only, and no PDF is re-rendered or committed tonight.

## Remaining whitelist

- Security: 262 263 (the children of #259, in this order)
- Code, step 2: 102 254 103
- Lessons, step 3: 118 120 122 124 126 128 130 132 135 137 138 139 140 141
  142 143 145 146 149 150 152 154 155 156 157 158 159 160 161 162 163 164
  165 166 167 168 169 170 171 172 173 174 175 176 178 180 182 183 184 186
  187 189 190 192 193 195 196 197 198 199 200 201 202 203 204 205 206 207
  208 209 210 211 212 213 214 215
- Content, step 4: 236 237 238 101 253 256 232 233 234
- Code, step 5: 58 84 85 112 111 98 220 228 221 68 69 70 71 56 252 67 231

## Parked

None yet.

## Single pull requests

- #259: decision task, no PR. Four decisions posted, all four findings valid and needing a fix. Filed #260, #261, #262 and #263 (all `ready-for-agent`). Closed.
- #260: PR #264, review `approve` on the first pass (one optional nit, skipped), CI green, merged. The CI run on `main` after the merge failed on the #255 flake (the examples sweep timed out at 5000 ms on the runner), which is unrelated to the change. The failed job was rerun and passed, and #255 started at once as the fix.
- #255: PR #265, review `approve` with one Minor (an inaccurate test comment) that went back as one commit, merged, CI on `main` green. #255 ran before #102 because the flake had just hit `main`.
- #261: PR #266, first review `needs changes` (two Majors: the Node hooks and `spell` ran an unpinned system `node`, and `install_args: bun` did not stop `mise run` from auto-installing every tool on the deploy runner; two Minors). The revision runs the three Node tools through the pinned `bun`, sets `MISE_TASK_RUN_AUTO_INSTALL` to false on the deploy build job, and keeps the `js-yaml` override with a reason (dropping it breaks the Astro pre-render step). Re-check `approve`, merged, CI and deploy on `main` green.
- #242: PR #267, review `approve` with three Minors (hydration wait, empty-id locator, solver kinds in the docs) that went back as one commit, merged, CI on `main` green. The dispatcher removed the two e2e collision notes from the wave lead template. #242 ran alongside #261 because it touches no gate or shared config.
- #241: PR #269, review `approve`, merged, CI on `main` green. The new check found 27 unlisted citations on 14 pages on `main` (13 plan files had an empty `sources` list), and the PR added every cited key. The review found a pre-existing plugin bug, listed below.
- #243: PR #270, review `approve`, merged, CI on `main` green. The builder kept the planned `after` rule ahead of the unblock score, which the issue's literal rule did not say but which its done-when needs (#140 in the Concepts slot). The reviewer agreed. The maintainer may want to confirm.

## Waves

None yet.

## Follow-ups for the maintainer

- `.mise.toml` `site-slides`: add an HTML-only flag (a `usage` `flag`) so the task runs on a machine without a working LaTeX toolchain. The reviewer of #264 notes that a positional-only spec binds `--html-only` as the deck path, so #263 needs a `flag "--html-only"` line in the spec.
- `.mise.toml:157`: mise's guidance for an arg with a default is `"${usage_deck?}"`, so `shellcheck` and a missing spec both surface. Optional nit from the #264 review.
- `README.md` line 49 and `docs/agents`: note that `site-slides` and `site-screenshot` take positional args (`mise tasks info <task>` shows the spec).
- `site/scripts/lib/examples.mjs:141`: `runFixture` calls `spawnSync` with no `timeout`, so a hung fixture hangs `mise run examples` and the Vitest sweep. Add one.
- `mise run ci` runs the fixture sweep twice (the `examples` task and the Vitest real-tree test). Consider dropping the Vitest one per `docs/agents/testing.md`.
- `docs/agents/testing.md`: say when a test takes a per-test timeout (long external-process sweeps) and point at the examples sweep as the pattern. Also note that two `site-build` runs in one worktree race on `node_modules/.vite/deps` and `.astro/`, so a load test needs a second worktree.
- `site/package.json`: bump `markdownlint-cli2` to 0.23.3 once it is 7 days old, and drop the `smol-toml` override when every parent asks for 1.7.1 or later. The `js-yaml` override is now load-bearing for the Astro pre-render bundle; revisit when `markdownlint-cli2` and Astro agree on a major.
- `.github/workflows/ci.yml`: add `site/bun.lock` to the prek cache key, since the Node hooks now come from the bun install.
- `AGENTS.md` "Supply chain": the "dependencies stay as ranges" bullet now carries a tools exception. A short "tools are exact, libraries are ranges" rule would read cleaner.
- `site/e2e/checkpoints.spec.ts`: derive the first-session example count (3) from `scanCheckpointTags` or a sibling `pageExamples`, so a new example needs no spec edit.
- `site/e2e/progress.spec.ts` and `due-counts.spec.ts`: derive the `how-models-work` seed's checkpoint ids and the `Review due: N items` count from `lessonCheckpoints`.
- `site/e2e/fixtures.ts`: `live-lessons.mjs:87` duplicates the id guard from `scripts/lib/checkpoints.mjs`, and `progress.spec.ts:53` mirrors the page's plural template exactly (a `> 1` guard with the literal plural would assert more). Two nits from the #267 review.
- `site/plugins/remark-citations.mjs:16`: the citation regex excludes a newline, so a citation wrapped over a soft line break renders as literal `(@...)` text and the new data check cannot see it. Five live citations wrap today: `concepts/same-prompt-twice.mdx:81`, `customizing-agents/settings-layering.mdx:58`, `using-agents/adjusting-the-brief.mdx:139` and `:276`, `using-agents/working-from-files.mdx:114`. Fix the plugin with a wrapped-case test. This is a visible content bug.
- `site/plugins/remark-citations.mjs`: `citationKeys` scans raw text, so a `(@key)` inside code would be counted by the data check and ignored by the build. No live page has that form. Move it to the shared tree walk if one ever does.
- Plan files: 13 live lessons had `sources: []` until #269. A review pass could add the sources consulted and set `sources-checked` and `review-by` where missing.
- `.mise.toml` `next-wave` description: add `--unblockers-first` to the listed flags.
- `.claude/skills/wave/SKILL.md` and `docs/agents/meta-orchestration.md`: decide whether `/wave` accepts `--unblockers-first` as a dispatcher argument (tonight the dispatcher passes it to the picker by hand).
- `site/tests/scripts/next-wave.test.ts:379`: the test title says "skipped or outside `--only`" but only the assigned path is exercised.
- `.vale/styles/ai-tells`: `SycophancyMarkers` flags "ahead of course position" as "of course".
- `site/scripts/lib/data.mjs:27`: header comment line at 105 columns.
