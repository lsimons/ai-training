# Session record, 2026-09-24, wave 11

The third code wave run by the meta-orchestration dispatcher
(`docs/agents/meta-orchestration.md`). The dispatcher chose and ordered the
ten issues by hand: #286, #111, #71, #275, #276, #278, #290, #287, #291 and
the wave 10 nits issue #292. Ten branches, one wave branch `wave/11-code`,
one pull request. Four of the issues change a gate or shared tooling on
purpose (#286, #276, #278 and #290).

## Counts

Ten builders, ten reviewers, fifteen review passes. The first review of #111
said needs changes. The other nine said approve with items to apply, and all
nine went back for one commit. The reviewers re-checked #286, #71, #275, #111
and #278, and all five approved. The lead read the diff instead of running
another review pass for the nits-only items (issue numbers 276, 287, 290, 291
and 292) and for the one-line nits after the #71 and #278 re-checks.
Zero merged with an open finding.

The builders ran six at a time: #286, #111, #71, #275, #276 and #287 first,
then #278 once #276 had pushed, #290 stacked on the #286 branch, and #291
and #292. The #290 builder rebased onto the #286 fix commit before its own nit
commit, and the lead rebased it onto the wave with `--onto` from the #286
branch. All ten rebases onto the wave branch went through without a conflict.

The lead made these calls:

- #111: `covered-by` goes in the lesson YAML (`lessonPlanSchema`) and spec
  S11, since lessons have no frontmatter. The Playwright step is skipped,
  because no lesson has the field and the issue forbids adding one.
  Whether `mise run data` should check the href joined the open maintainer
  question #288.
- #71: spec S08 "Recommended flags" drops `--safe-mode`, which isn't a
  Claude Code flag, and names what the page recommends.
- #290: `remark-checkpoints.mjs` stops writing the unused `checkpoints`
  frontmatter entry, and `lessons.ts` still parses the body itself, synchronously.
- #278: a separate `vuln.yml` workflow instead of conditions in `ci.yml`.
- #276: the eval package check is in Python and names
  `vale sync --config .vale-eval.ini`, since `prose-sync` syncs only
  `.vale.ini`.

## What review caught

- The #111 tip link rendered as a block of its own, because Starlight's
  right sidebar styles every `a` as `display: block`. The reviewer found it
  in a screenshot of a built page. The Container API tests don't see CSS.
- The #71 page said Manual mode asks before every command (a settings allow
  rule skips the prompt) and that the tutor asks for a reinstall whenever
  the site changes (only on a skill `version` bump). The review also found
  that spec S08 still recommended the nonexistent `--safe-mode` flag.
- `renderInline` in #275 turned `2 * 3 * 4` in a competency behavior into
  emphasis. The fix tightened the rule and pinned the remaining differences
  from remark in tests.
- The ported #286 gate skipped `run=""` silently, where `main` failed it,
  and a parse error in the data check didn't name the file. The builder found seven
  tags in four lessons that failed the new gate, where the issue named two.
- The #278 docs missed that GitHub disables a scheduled workflow after 60
  days without repository activity. The first fix then claimed an email
  that GitHub's docs don't mention.
- The #287 groups nested two `nav` landmarks inside Starlight's mobile `nav`.
- The #291 in-viewport assertion would pass without a scroll if the target
  was already on screen.

## What the builder prompt should have said

- A CSS rule for markup inside a Starlight sidebar gets checked in the
  built page at the target width, because Starlight's own styles apply
  there and no unit test sees them.
- A sentence about vendor behavior (a CLI flag, a GitHub notification)
  names the vendor page that states it. The #278 email claim and the #71
  "every command" claim had no source.
- The gate's result is the truth over the list in the issue body. #286 named
  two lessons and seven tags failed, and #275 placed `referenceText` in the
  wrong file.
- An in-viewport assertion after a click needs the not-in-viewport
  assertion before it.

## Filed

- #294 Show the covered-by skip tip in the mobile menu
- #295 prose: one Vale package parser for the prose and eval checks, and a
  prose-eval-sync task
- #296 data: reject Markdown forms in competency behaviors that the
  behavior renderer does not support
- #297 Cosmetic nits left open on wave 11 branches

Follow-ups: none.
